# Auto-generated by codegen.py — do not edit manually
# Generated: 2026-04-22 20:46:20
# Generated file exception:
# This file is machine-written from sheet data, so it may use compact Python.
# We keep the sheet-driven structure here because codegen.py rebuilds it.
# This is safe because codegen.py rebuilds this file from the same source data.

from typing import TYPE_CHECKING
from worlds.generic.Rules import add_rule
from .Locations import (
    ITEM_SHARED_LOCATION_GROUPS,
    MISSION_CLEAR_MODE_PER_MAP,
    is_valid_location,
)

if TYPE_CHECKING:
    from . import GoldeneyeWorld


BULLET_GUN_NAMES = [
    "Klobb",
    "PP7 Special Issue",
    "PP7 (Silenced)",
    "DD44 Dostovei",
    "KF7 Soviet",
    "ZMG (9 mm)",
    "D5K Deutsche",
    "D5K (Silenced)",
    "Sniper Rifle",
    "Phantom",
    "AR33 Assault Rifle",
    "RC-P90",
    "Shotgun",
    "Cougar Magnum",
    "Silver PP7",
    "Automatic Shotgun",
    "Golden Gun",
    "Gold PP7",
]


GUN_PROGRESSIVE_COUNT = 1
EXPLOSIVE_PROGRESSIVE_COUNT = 15


EXPLOSIVE_ITEM_NAMES = [
    "Timed Mine",
    "Proximity Mine",
    "Remote Mine",
    "Grenade Launcher",
    "Rocket Launcher",
    "Tank (Missiles)",
]


def has_gun(state, player: int) -> bool:
    """True if player has any gun item or enough Progressive Weapons for guns."""
    return (
        any(state.has(gun, player) for gun in BULLET_GUN_NAMES)
        or state.has("Progressive Weapon", player, GUN_PROGRESSIVE_COUNT)
    )


def has_explosive(state, player: int) -> bool:
    """True if player has any explosive item or enough Progressive Weapons for explosives."""
    return (
        any(state.has(item_name, player) for item_name in EXPLOSIVE_ITEM_NAMES)
        or state.has("Progressive Weapon", player, EXPLOSIVE_PROGRESSIVE_COUNT)
    )


def set_rules(world: "GoldeneyeWorld") -> None:
    player = world.player

    # Mission entrance rules
    add_rule(world.multiworld.get_entrance("Menu -> Dam", player),
             lambda state: state.has("Dam", player))
    add_rule(world.multiworld.get_entrance("Menu -> Facility", player),
             lambda state: state.has("Facility", player))
    add_rule(world.multiworld.get_entrance("Menu -> Runway", player),
             lambda state: state.has("Runway", player))
    add_rule(world.multiworld.get_entrance("Menu -> Surface 1", player),
             lambda state: state.has("Surface 1", player))
    add_rule(world.multiworld.get_entrance("Menu -> Bunker 1", player),
             lambda state: (
                 state.has("Bunker 1", player) and
                 has_gun(state, player)
             ))
    add_rule(world.multiworld.get_entrance("Menu -> Silo", player),
             lambda state: (
                 state.has("Silo", player) and
                 has_gun(state, player)
             ))
    add_rule(world.multiworld.get_entrance("Menu -> Frigate", player),
             lambda state: state.has("Frigate", player))
    add_rule(world.multiworld.get_entrance("Menu -> Surface 2", player),
             lambda state: state.has("Surface 2", player))
    add_rule(world.multiworld.get_entrance("Menu -> Bunker 2", player),
             lambda state: (
                 state.has("Bunker 2", player) and
                 state.has("Watch Magnet (Bunker 2 / Archives Loadout)", player) and
                 state.has("Cell Key 1 (Bunker 2 Item)", player)
             ))
    add_rule(world.multiworld.get_entrance("Menu -> Statue", player),
             lambda state: state.has("Statue", player))
    add_rule(world.multiworld.get_entrance("Menu -> Archives", player),
             lambda state: state.has("Archives", player))
    add_rule(world.multiworld.get_entrance("Menu -> Streets", player),
             lambda state: state.has("Streets", player))
    add_rule(world.multiworld.get_entrance("Menu -> Depot", player),
             lambda state: state.has("Depot", player))
    add_rule(world.multiworld.get_entrance("Menu -> Train", player),
             lambda state: state.has("Train", player))
    add_rule(world.multiworld.get_entrance("Menu -> Jungle", player),
             lambda state: (
                 state.has("Jungle", player) and
                 has_gun(state, player)
             ))
    add_rule(world.multiworld.get_entrance("Menu -> Control", player),
             lambda state: (
                 state.has("Control", player) and
                 has_gun(state, player)
             ))
    add_rule(world.multiworld.get_entrance("Menu -> Caverns", player),
             lambda state: (
                 state.has("Caverns", player) and
                 has_gun(state, player)
             ))
    add_rule(world.multiworld.get_entrance("Menu -> Cradle", player),
             lambda state: (
                 state.has("Cradle", player) and
                 has_gun(state, player)
             ))
    add_rule(world.multiworld.get_entrance("Menu -> Aztec", player),
             lambda state: (
                 state.has("Aztec", player) and
                 has_gun(state, player)
             ))
    add_rule(world.multiworld.get_entrance("Menu -> Egyptian", player),
             lambda state: (
                 state.has("Egyptian", player) and
                 has_gun(state, player)
             ))

    # Location rules
    if "Archives - (Clear)" in world.location_name_to_id and is_valid_location(world, "Archives - (Clear)"):
        add_rule(world.multiworld.get_location("Archives - (Clear)", player),
                 lambda state: (
                     state.has("Interrogation Room Key (Archives Item)", player) and
                     has_gun(state, player)
                 ))
    if "Archives - Agent (Clear)" in world.location_name_to_id and is_valid_location(world, "Archives - Agent (Clear)"):
        add_rule(world.multiworld.get_location("Archives - Agent (Clear)", player),
                 lambda state: (
                     state.has("Interrogation Room Key (Archives Item)", player) and
                     has_gun(state, player)
                 ))
    if "Archives - Double Agent (Clear)" in world.location_name_to_id and is_valid_location(world, "Archives - Double Agent (Clear)"):
        add_rule(world.multiworld.get_location("Archives - Double Agent (Clear)", player),
                 lambda state: (
                     state.has("Interrogation Room Key (Archives Item)", player) and
                     state.has("Archives Safe Key (Archives Item)", player) and
                     state.has("Flight Recorder (Archives Item)", player) and
                     has_gun(state, player)
                 ))
    if "Archives - Escape From Interrogation Room" in world.location_name_to_id and is_valid_location(world, "Archives - Escape From Interrogation Room"):
        add_rule(world.multiworld.get_location("Archives - Escape From Interrogation Room", player),
                 lambda state: has_gun(state, player))
    if "Archives - Escape From Interrogation Room (Agent)" in world.location_name_to_id and is_valid_location(world, "Archives - Escape From Interrogation Room (Agent)"):
        add_rule(world.multiworld.get_location("Archives - Escape From Interrogation Room (Agent)", player),
                 lambda state: (
                     state.has("Interrogation Room Key (Archives Item)", player) and
                     has_gun(state, player)
                 ))
    if "Archives - Escape From Interrogation Room (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Archives - Escape From Interrogation Room (Double Agent)"):
        add_rule(world.multiworld.get_location("Archives - Escape From Interrogation Room (Double Agent)", player),
                 lambda state: (
                     state.has("Interrogation Room Key (Archives Item)", player) and
                     has_gun(state, player)
                 ))
    if "Archives - Escape From Interrogation Room (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Archives - Escape From Interrogation Room (Secret Agent)"):
        add_rule(world.multiworld.get_location("Archives - Escape From Interrogation Room (Secret Agent)", player),
                 lambda state: (
                     state.has("Interrogation Room Key (Archives Item)", player) and
                     has_gun(state, player)
                 ))
    if "Archives - Escape With Natalya Again" in world.location_name_to_id and is_valid_location(world, "Archives - Escape With Natalya Again"):
        add_rule(world.multiworld.get_location("Archives - Escape With Natalya Again", player),
                 lambda state: has_gun(state, player))
    if "Archives - Escape With Natalya Again (Agent)" in world.location_name_to_id and is_valid_location(world, "Archives - Escape With Natalya Again (Agent)"):
        add_rule(world.multiworld.get_location("Archives - Escape With Natalya Again (Agent)", player),
                 lambda state: (
                     state.has("Interrogation Room Key (Archives Item)", player) and
                     has_gun(state, player)
                 ))
    if "Archives - Escape With Natalya Again (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Archives - Escape With Natalya Again (Double Agent)"):
        add_rule(world.multiworld.get_location("Archives - Escape With Natalya Again (Double Agent)", player),
                 lambda state: (
                     state.has("Interrogation Room Key (Archives Item)", player) and
                     has_gun(state, player)
                 ))
    if "Archives - Escape With Natalya Again (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Archives - Escape With Natalya Again (Secret Agent)"):
        add_rule(world.multiworld.get_location("Archives - Escape With Natalya Again (Secret Agent)", player),
                 lambda state: (
                     state.has("Interrogation Room Key (Archives Item)", player) and
                     has_gun(state, player)
                 ))
    if "Archives - Find Natalya" in world.location_name_to_id and is_valid_location(world, "Archives - Find Natalya"):
        add_rule(world.multiworld.get_location("Archives - Find Natalya", player),
                 lambda state: has_gun(state, player))
    if "Archives - Find Natalya (Agent)" in world.location_name_to_id and is_valid_location(world, "Archives - Find Natalya (Agent)"):
        add_rule(world.multiworld.get_location("Archives - Find Natalya (Agent)", player),
                 lambda state: (
                     state.has("Interrogation Room Key (Archives Item)", player) and
                     has_gun(state, player)
                 ))
    if "Archives - Find Natalya (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Archives - Find Natalya (Double Agent)"):
        add_rule(world.multiworld.get_location("Archives - Find Natalya (Double Agent)", player),
                 lambda state: (
                     state.has("Interrogation Room Key (Archives Item)", player) and
                     has_gun(state, player)
                 ))
    if "Archives - Find Natalya (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Archives - Find Natalya (Secret Agent)"):
        add_rule(world.multiworld.get_location("Archives - Find Natalya (Secret Agent)", player),
                 lambda state: (
                     state.has("Interrogation Room Key (Archives Item)", player) and
                     has_gun(state, player)
                 ))
    if "Archives - Flight Recorder (Agent)" in world.location_name_to_id and is_valid_location(world, "Archives - Flight Recorder (Agent)"):
        add_rule(world.multiworld.get_location("Archives - Flight Recorder (Agent)", player),
                 lambda state: (
                     state.has("Interrogation Room Key (Archives Item)", player) and
                     state.has("Archives Safe Key (Archives Item)", player) and
                     has_gun(state, player)
                 ))
    if "Archives - Flight Recorder (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Archives - Flight Recorder (Double Agent)"):
        add_rule(world.multiworld.get_location("Archives - Flight Recorder (Double Agent)", player),
                 lambda state: (
                     state.has("Interrogation Room Key (Archives Item)", player) and
                     state.has("Archives Safe Key (Archives Item)", player) and
                     has_gun(state, player)
                 ))
    if "Archives - Flight Recorder (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Archives - Flight Recorder (Secret Agent)"):
        add_rule(world.multiworld.get_location("Archives - Flight Recorder (Secret Agent)", player),
                 lambda state: (
                     state.has("Interrogation Room Key (Archives Item)", player) and
                     state.has("Archives Safe Key (Archives Item)", player) and
                     has_gun(state, player)
                 ))
    if "Archives - Interrogation Room Key (Agent)" in world.location_name_to_id and is_valid_location(world, "Archives - Interrogation Room Key (Agent)"):
        add_rule(world.multiworld.get_location("Archives - Interrogation Room Key (Agent)", player),
                 lambda state: has_gun(state, player))
    if "Archives - Interrogation Room Key (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Archives - Interrogation Room Key (Double Agent)"):
        add_rule(world.multiworld.get_location("Archives - Interrogation Room Key (Double Agent)", player),
                 lambda state: has_gun(state, player))
    if "Archives - Interrogation Room Key (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Archives - Interrogation Room Key (Secret Agent)"):
        add_rule(world.multiworld.get_location("Archives - Interrogation Room Key (Secret Agent)", player),
                 lambda state: has_gun(state, player))
    if "Archives - PP7" in world.location_name_to_id and is_valid_location(world, "Archives - PP7"):
        add_rule(world.multiworld.get_location("Archives - PP7", player),
                 lambda state: has_gun(state, player))
    if "Archives - PP7 (Agent)" in world.location_name_to_id and is_valid_location(world, "Archives - PP7 (Agent)"):
        add_rule(world.multiworld.get_location("Archives - PP7 (Agent)", player),
                 lambda state: has_gun(state, player))
    if "Archives - PP7 (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Archives - PP7 (Double Agent)"):
        add_rule(world.multiworld.get_location("Archives - PP7 (Double Agent)", player),
                 lambda state: has_gun(state, player))
    if "Archives - PP7 (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Archives - PP7 (Secret Agent)"):
        add_rule(world.multiworld.get_location("Archives - PP7 (Secret Agent)", player),
                 lambda state: has_gun(state, player))
    if "Archives - Recover Helicopter Black Box" in world.location_name_to_id and is_valid_location(world, "Archives - Recover Helicopter Black Box"):
        add_rule(world.multiworld.get_location("Archives - Recover Helicopter Black Box", player),
                 lambda state: has_gun(state, player))
    if "Archives - Recover Helicopter Black Box (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Archives - Recover Helicopter Black Box (Double Agent)"):
        add_rule(world.multiworld.get_location("Archives - Recover Helicopter Black Box (Double Agent)", player),
                 lambda state: (
                     state.has("Interrogation Room Key (Archives Item)", player) and
                     state.has("Archives Safe Key (Archives Item)", player) and
                     state.has("Flight Recorder (Archives Item)", player) and
                     has_gun(state, player)
                 ))
    if "Archives - Recover Helicopter Black Box (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Archives - Recover Helicopter Black Box (Secret Agent)"):
        add_rule(world.multiworld.get_location("Archives - Recover Helicopter Black Box (Secret Agent)", player),
                 lambda state: (
                     state.has("Interrogation Room Key (Archives Item)", player) and
                     state.has("Archives Safe Key (Archives Item)", player) and
                     state.has("Flight Recorder (Archives Item)", player) and
                     has_gun(state, player)
                 ))
    if "Archives - Safe Key (Agent)" in world.location_name_to_id and is_valid_location(world, "Archives - Safe Key (Agent)"):
        add_rule(world.multiworld.get_location("Archives - Safe Key (Agent)", player),
                 lambda state: (
                     state.has("Interrogation Room Key (Archives Item)", player) and
                     has_gun(state, player)
                 ))
    if "Archives - Safe Key (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Archives - Safe Key (Double Agent)"):
        add_rule(world.multiworld.get_location("Archives - Safe Key (Double Agent)", player),
                 lambda state: (
                     state.has("Interrogation Room Key (Archives Item)", player) and
                     has_gun(state, player)
                 ))
    if "Archives - Safe Key (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Archives - Safe Key (Secret Agent)"):
        add_rule(world.multiworld.get_location("Archives - Safe Key (Secret Agent)", player),
                 lambda state: (
                     state.has("Interrogation Room Key (Archives Item)", player) and
                     has_gun(state, player)
                 ))
    if "Archives - Secret Agent (Clear)" in world.location_name_to_id and is_valid_location(world, "Archives - Secret Agent (Clear)"):
        add_rule(world.multiworld.get_location("Archives - Secret Agent (Clear)", player),
                 lambda state: (
                     state.has("Interrogation Room Key (Archives Item)", player) and
                     state.has("Archives Safe Key (Archives Item)", player) and
                     state.has("Flight Recorder (Archives Item)", player) and
                     has_gun(state, player)
                 ))
    if "Aztec - (Clear)" in world.location_name_to_id and is_valid_location(world, "Aztec - (Clear)"):
        add_rule(world.multiworld.get_location("Aztec - (Clear)", player),
                 lambda state: (
                     state.has("Guidance Data (Aztec Item)", player) and
                     state.has("Security Smart Card (Aztec Item)", player) and
                     state.has("Launch Protocol Data (Aztec Item)", player) and
                     has_gun(state, player)
                 ))
    if "Aztec - Agent (Clear)" in world.location_name_to_id and is_valid_location(world, "Aztec - Agent (Clear)"):
        add_rule(world.multiworld.get_location("Aztec - Agent (Clear)", player),
                 lambda state: (
                     state.has("Guidance Data (Aztec Item)", player) and
                     state.has("Security Smart Card (Aztec Item)", player) and
                     state.has("Launch Protocol Data (Aztec Item)", player) and
                     has_gun(state, player)
                 ))
    if "Aztec - Double Agent (Clear)" in world.location_name_to_id and is_valid_location(world, "Aztec - Double Agent (Clear)"):
        add_rule(world.multiworld.get_location("Aztec - Double Agent (Clear)", player),
                 lambda state: (
                     state.has("Guidance Data (Aztec Item)", player) and
                     state.has("Security Smart Card (Aztec Item)", player) and
                     state.has("Launch Protocol Data (Aztec Item)", player) and
                     has_gun(state, player)
                 ))
    if "Aztec - Guidance Data (Agent)" in world.location_name_to_id and is_valid_location(world, "Aztec - Guidance Data (Agent)"):
        add_rule(world.multiworld.get_location("Aztec - Guidance Data (Agent)", player),
                 lambda state: has_gun(state, player))
    if "Aztec - Guidance Data (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Aztec - Guidance Data (Double Agent)"):
        add_rule(world.multiworld.get_location("Aztec - Guidance Data (Double Agent)", player),
                 lambda state: has_gun(state, player))
    if "Aztec - Guidance Data (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Aztec - Guidance Data (Secret Agent)"):
        add_rule(world.multiworld.get_location("Aztec - Guidance Data (Secret Agent)", player),
                 lambda state: has_gun(state, player))
    if "Aztec - Launch Protocol Data (Agent)" in world.location_name_to_id and is_valid_location(world, "Aztec - Launch Protocol Data (Agent)"):
        add_rule(world.multiworld.get_location("Aztec - Launch Protocol Data (Agent)", player),
                 lambda state: (
                     state.has("Security Smart Card (Aztec Item)", player) and
                     has_gun(state, player)
                 ))
    if "Aztec - Launch Protocol Data (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Aztec - Launch Protocol Data (Double Agent)"):
        add_rule(world.multiworld.get_location("Aztec - Launch Protocol Data (Double Agent)", player),
                 lambda state: (
                     state.has("Security Smart Card (Aztec Item)", player) and
                     has_gun(state, player)
                 ))
    if "Aztec - Launch Protocol Data (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Aztec - Launch Protocol Data (Secret Agent)"):
        add_rule(world.multiworld.get_location("Aztec - Launch Protocol Data (Secret Agent)", player),
                 lambda state: (
                     state.has("Security Smart Card (Aztec Item)", player) and
                     has_gun(state, player)
                 ))
    if "Aztec - Launch Shuttle" in world.location_name_to_id and is_valid_location(world, "Aztec - Launch Shuttle"):
        add_rule(world.multiworld.get_location("Aztec - Launch Shuttle", player),
                 lambda state: has_gun(state, player))
    if "Aztec - Launch Shuttle (Agent)" in world.location_name_to_id and is_valid_location(world, "Aztec - Launch Shuttle (Agent)"):
        add_rule(world.multiworld.get_location("Aztec - Launch Shuttle (Agent)", player),
                 lambda state: (
                     state.has("Guidance Data (Aztec Item)", player) and
                     state.has("Security Smart Card (Aztec Item)", player) and
                     state.has("Launch Protocol Data (Aztec Item)", player) and
                     has_gun(state, player)
                 ))
    if "Aztec - Launch Shuttle (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Aztec - Launch Shuttle (Double Agent)"):
        add_rule(world.multiworld.get_location("Aztec - Launch Shuttle (Double Agent)", player),
                 lambda state: (
                     state.has("Guidance Data (Aztec Item)", player) and
                     state.has("Security Smart Card (Aztec Item)", player) and
                     state.has("Launch Protocol Data (Aztec Item)", player) and
                     has_gun(state, player)
                 ))
    if "Aztec - Launch Shuttle (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Aztec - Launch Shuttle (Secret Agent)"):
        add_rule(world.multiworld.get_location("Aztec - Launch Shuttle (Secret Agent)", player),
                 lambda state: (
                     state.has("Guidance Data (Aztec Item)", player) and
                     state.has("Security Smart Card (Aztec Item)", player) and
                     state.has("Launch Protocol Data (Aztec Item)", player) and
                     has_gun(state, player)
                 ))
    if "Aztec - Reprogram Shuttle Guidance" in world.location_name_to_id and is_valid_location(world, "Aztec - Reprogram Shuttle Guidance"):
        add_rule(world.multiworld.get_location("Aztec - Reprogram Shuttle Guidance", player),
                 lambda state: has_gun(state, player))
    if "Aztec - Reprogram Shuttle Guidance (Agent)" in world.location_name_to_id and is_valid_location(world, "Aztec - Reprogram Shuttle Guidance (Agent)"):
        add_rule(world.multiworld.get_location("Aztec - Reprogram Shuttle Guidance (Agent)", player),
                 lambda state: (
                     state.has("Guidance Data (Aztec Item)", player) and
                     state.has("Security Smart Card (Aztec Item)", player) and
                     has_gun(state, player)
                 ))
    if "Aztec - Reprogram Shuttle Guidance (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Aztec - Reprogram Shuttle Guidance (Double Agent)"):
        add_rule(world.multiworld.get_location("Aztec - Reprogram Shuttle Guidance (Double Agent)", player),
                 lambda state: (
                     state.has("Guidance Data (Aztec Item)", player) and
                     state.has("Security Smart Card (Aztec Item)", player) and
                     has_gun(state, player)
                 ))
    if "Aztec - Reprogram Shuttle Guidance (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Aztec - Reprogram Shuttle Guidance (Secret Agent)"):
        add_rule(world.multiworld.get_location("Aztec - Reprogram Shuttle Guidance (Secret Agent)", player),
                 lambda state: (
                     state.has("Guidance Data (Aztec Item)", player) and
                     state.has("Security Smart Card (Aztec Item)", player) and
                     has_gun(state, player)
                 ))
    if "Aztec - Secret Agent (Clear)" in world.location_name_to_id and is_valid_location(world, "Aztec - Secret Agent (Clear)"):
        add_rule(world.multiworld.get_location("Aztec - Secret Agent (Clear)", player),
                 lambda state: (
                     state.has("Guidance Data (Aztec Item)", player) and
                     state.has("Security Smart Card (Aztec Item)", player) and
                     state.has("Launch Protocol Data (Aztec Item)", player) and
                     has_gun(state, player)
                 ))
    if "Aztec - Security Smart Card (Agent)" in world.location_name_to_id and is_valid_location(world, "Aztec - Security Smart Card (Agent)"):
        add_rule(world.multiworld.get_location("Aztec - Security Smart Card (Agent)", player),
                 lambda state: has_gun(state, player))
    if "Aztec - Security Smart Card (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Aztec - Security Smart Card (Double Agent)"):
        add_rule(world.multiworld.get_location("Aztec - Security Smart Card (Double Agent)", player),
                 lambda state: has_gun(state, player))
    if "Aztec - Security Smart Card (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Aztec - Security Smart Card (Secret Agent)"):
        add_rule(world.multiworld.get_location("Aztec - Security Smart Card (Secret Agent)", player),
                 lambda state: has_gun(state, player))
    if "Bunker 1 - (Clear)" in world.location_name_to_id and is_valid_location(world, "Bunker 1 - (Clear)"):
        add_rule(world.multiworld.get_location("Bunker 1 - (Clear)", player),
                 lambda state: (
                     state.has("Key Analyzer Case (Bunker 1 Loadout)", player) and
                     state.has("Goldeneye Key (Bunker 1 Item)", player) and
                     state.has("Camera (Bunker 1 / Silo Loadout)", player) and
                     state.has("Security Room Keycard (Bunker 1 Item)", player) and
                     has_gun(state, player)
                 ))
    if "Bunker 1 - Agent (Clear)" in world.location_name_to_id and is_valid_location(world, "Bunker 1 - Agent (Clear)"):
        add_rule(world.multiworld.get_location("Bunker 1 - Agent (Clear)", player),
                 lambda state: (
                     state.has("Key Analyzer Case (Bunker 1 Loadout)", player) and
                     state.has("Goldeneye Key (Bunker 1 Item)", player) and
                     state.has("Camera (Bunker 1 / Silo Loadout)", player) and
                     state.has("Security Room Keycard (Bunker 1 Item)", player) and
                     has_gun(state, player)
                 ))
    if "Bunker 1 - Computer Room Keycard (Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 1 - Computer Room Keycard (Agent)"):
        add_rule(world.multiworld.get_location("Bunker 1 - Computer Room Keycard (Agent)", player),
                 lambda state: has_gun(state, player))
    if "Bunker 1 - Computer Room Keycard (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 1 - Computer Room Keycard (Double Agent)"):
        add_rule(world.multiworld.get_location("Bunker 1 - Computer Room Keycard (Double Agent)", player),
                 lambda state: has_gun(state, player))
    if "Bunker 1 - Computer Room Keycard (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 1 - Computer Room Keycard (Secret Agent)"):
        add_rule(world.multiworld.get_location("Bunker 1 - Computer Room Keycard (Secret Agent)", player),
                 lambda state: has_gun(state, player))
    if "Bunker 1 - Copy Goldeneye Key And Leave Original" in world.location_name_to_id and is_valid_location(world, "Bunker 1 - Copy Goldeneye Key And Leave Original"):
        add_rule(world.multiworld.get_location("Bunker 1 - Copy Goldeneye Key And Leave Original", player),
                 lambda state: has_gun(state, player))
    if "Bunker 1 - Copy Goldeneye Key And Leave Original (Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 1 - Copy Goldeneye Key And Leave Original (Agent)"):
        add_rule(world.multiworld.get_location("Bunker 1 - Copy Goldeneye Key And Leave Original (Agent)", player),
                 lambda state: (
                     state.has("Key Analyzer Case (Bunker 1 Loadout)", player) and
                     state.has("Goldeneye Key (Bunker 1 Item)", player) and
                     has_gun(state, player)
                 ))
    if "Bunker 1 - Copy Goldeneye Key And Leave Original (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 1 - Copy Goldeneye Key And Leave Original (Double Agent)"):
        add_rule(world.multiworld.get_location("Bunker 1 - Copy Goldeneye Key And Leave Original (Double Agent)", player),
                 lambda state: (
                     state.has("Key Analyzer Case (Bunker 1 Loadout)", player) and
                     state.has("Goldeneye Key (Bunker 1 Item)", player) and
                     has_gun(state, player)
                 ))
    if "Bunker 1 - Copy Goldeneye Key And Leave Original (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 1 - Copy Goldeneye Key And Leave Original (Secret Agent)"):
        add_rule(world.multiworld.get_location("Bunker 1 - Copy Goldeneye Key And Leave Original (Secret Agent)", player),
                 lambda state: (
                     state.has("Key Analyzer Case (Bunker 1 Loadout)", player) and
                     state.has("Goldeneye Key (Bunker 1 Item)", player) and
                     has_gun(state, player)
                 ))
    if "Bunker 1 - Disrupt All Surveillance Equipment" in world.location_name_to_id and is_valid_location(world, "Bunker 1 - Disrupt All Surveillance Equipment"):
        add_rule(world.multiworld.get_location("Bunker 1 - Disrupt All Surveillance Equipment", player),
                 lambda state: has_gun(state, player))
    if "Bunker 1 - Disrupt All Surveillance Equipment (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 1 - Disrupt All Surveillance Equipment (Double Agent)"):
        add_rule(world.multiworld.get_location("Bunker 1 - Disrupt All Surveillance Equipment (Double Agent)", player),
                 lambda state: has_gun(state, player))
    if "Bunker 1 - Disrupt All Surveillance Equipment (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 1 - Disrupt All Surveillance Equipment (Secret Agent)"):
        add_rule(world.multiworld.get_location("Bunker 1 - Disrupt All Surveillance Equipment (Secret Agent)", player),
                 lambda state: has_gun(state, player))
    if "Bunker 1 - Double Agent (Clear)" in world.location_name_to_id and is_valid_location(world, "Bunker 1 - Double Agent (Clear)"):
        add_rule(world.multiworld.get_location("Bunker 1 - Double Agent (Clear)", player),
                 lambda state: (
                     state.has("Key Analyzer Case (Bunker 1 Loadout)", player) and
                     state.has("Goldeneye Key (Bunker 1 Item)", player) and
                     state.has("Camera (Bunker 1 / Silo Loadout)", player) and
                     state.has("Security Room Keycard (Bunker 1 Item)", player) and
                     state.has("Computer Room Keycard (Bunker 1 Item)", player) and
                     state.has("Datathief (Bunker 1 Loadout)", player) and
                     has_gun(state, player)
                 ))
    if "Bunker 1 - Download Data From Computer" in world.location_name_to_id and is_valid_location(world, "Bunker 1 - Download Data From Computer"):
        add_rule(world.multiworld.get_location("Bunker 1 - Download Data From Computer", player),
                 lambda state: has_gun(state, player))
    if "Bunker 1 - Download Data From Computer (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 1 - Download Data From Computer (Double Agent)"):
        add_rule(world.multiworld.get_location("Bunker 1 - Download Data From Computer (Double Agent)", player),
                 lambda state: (
                     state.has("Computer Room Keycard (Bunker 1 Item)", player) and
                     state.has("Datathief (Bunker 1 Loadout)", player) and
                     has_gun(state, player)
                 ))
    if "Bunker 1 - Get Personnel To Activate Computer" in world.location_name_to_id and is_valid_location(world, "Bunker 1 - Get Personnel To Activate Computer"):
        add_rule(world.multiworld.get_location("Bunker 1 - Get Personnel To Activate Computer", player),
                 lambda state: has_gun(state, player))
    if "Bunker 1 - Get Personnel To Activate Computer (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 1 - Get Personnel To Activate Computer (Double Agent)"):
        add_rule(world.multiworld.get_location("Bunker 1 - Get Personnel To Activate Computer (Double Agent)", player),
                 lambda state: (
                     state.has("Computer Room Keycard (Bunker 1 Item)", player) and
                     has_gun(state, player)
                 ))
    if "Bunker 1 - Goldeneye Key (Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 1 - Goldeneye Key (Agent)"):
        add_rule(world.multiworld.get_location("Bunker 1 - Goldeneye Key (Agent)", player),
                 lambda state: has_gun(state, player))
    if "Bunker 1 - Goldeneye Key (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 1 - Goldeneye Key (Double Agent)"):
        add_rule(world.multiworld.get_location("Bunker 1 - Goldeneye Key (Double Agent)", player),
                 lambda state: has_gun(state, player))
    if "Bunker 1 - Goldeneye Key (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 1 - Goldeneye Key (Secret Agent)"):
        add_rule(world.multiworld.get_location("Bunker 1 - Goldeneye Key (Secret Agent)", player),
                 lambda state: has_gun(state, player))
    if "Bunker 1 - Photograph Main Video Screen" in world.location_name_to_id and is_valid_location(world, "Bunker 1 - Photograph Main Video Screen"):
        add_rule(world.multiworld.get_location("Bunker 1 - Photograph Main Video Screen", player),
                 lambda state: has_gun(state, player))
    if "Bunker 1 - Photograph Main Video Screen (Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 1 - Photograph Main Video Screen (Agent)"):
        add_rule(world.multiworld.get_location("Bunker 1 - Photograph Main Video Screen (Agent)", player),
                 lambda state: (
                     state.has("Camera (Bunker 1 / Silo Loadout)", player) and
                     has_gun(state, player)
                 ))
    if "Bunker 1 - Photograph Main Video Screen (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 1 - Photograph Main Video Screen (Double Agent)"):
        add_rule(world.multiworld.get_location("Bunker 1 - Photograph Main Video Screen (Double Agent)", player),
                 lambda state: (
                     state.has("Camera (Bunker 1 / Silo Loadout)", player) and
                     has_gun(state, player)
                 ))
    if "Bunker 1 - Photograph Main Video Screen (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 1 - Photograph Main Video Screen (Secret Agent)"):
        add_rule(world.multiworld.get_location("Bunker 1 - Photograph Main Video Screen (Secret Agent)", player),
                 lambda state: (
                     state.has("Camera (Bunker 1 / Silo Loadout)", player) and
                     has_gun(state, player)
                 ))
    if "Bunker 1 - Secret Agent (Clear)" in world.location_name_to_id and is_valid_location(world, "Bunker 1 - Secret Agent (Clear)"):
        add_rule(world.multiworld.get_location("Bunker 1 - Secret Agent (Clear)", player),
                 lambda state: (
                     state.has("Key Analyzer Case (Bunker 1 Loadout)", player) and
                     state.has("Goldeneye Key (Bunker 1 Item)", player) and
                     state.has("Camera (Bunker 1 / Silo Loadout)", player) and
                     state.has("Security Room Keycard (Bunker 1 Item)", player) and
                     has_gun(state, player)
                 ))
    if "Bunker 1 - Security Keycard (Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 1 - Security Keycard (Agent)"):
        add_rule(world.multiworld.get_location("Bunker 1 - Security Keycard (Agent)", player),
                 lambda state: has_gun(state, player))
    if "Bunker 1 - Security Keycard (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 1 - Security Keycard (Double Agent)"):
        add_rule(world.multiworld.get_location("Bunker 1 - Security Keycard (Double Agent)", player),
                 lambda state: has_gun(state, player))
    if "Bunker 1 - Security Keycard (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 1 - Security Keycard (Secret Agent)"):
        add_rule(world.multiworld.get_location("Bunker 1 - Security Keycard (Secret Agent)", player),
                 lambda state: has_gun(state, player))
    if "Bunker 2 - (Clear)" in world.location_name_to_id and is_valid_location(world, "Bunker 2 - (Clear)"):
        add_rule(world.multiworld.get_location("Bunker 2 - (Clear)", player),
                 lambda state: (
                     state.has("Watch Magnet (Bunker 2 / Archives Loadout)", player) and
                     state.has("Cell Key 1 (Bunker 2 Item)", player) and
                     state.has("Cell Key 2 (Bunker 2 Item)", player) and
                     state.has("CCTV Tape (Bunker 2 Item)", player) and
                     state.has("Keycard B (Bunker 2 Item)", player)
                 ))
    if "Bunker 2 - Agent (Clear)" in world.location_name_to_id and is_valid_location(world, "Bunker 2 - Agent (Clear)"):
        add_rule(world.multiworld.get_location("Bunker 2 - Agent (Clear)", player),
                 lambda state: (
                     state.has("Watch Magnet (Bunker 2 / Archives Loadout)", player) and
                     state.has("Cell Key 1 (Bunker 2 Item)", player) and
                     state.has("Cell Key 2 (Bunker 2 Item)", player) and
                     state.has("CCTV Tape (Bunker 2 Item)", player) and
                     state.has("Keycard B (Bunker 2 Item)", player)
                 ))
    if "Bunker 2 - CCTV Tape (Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 2 - CCTV Tape (Agent)"):
        add_rule(world.multiworld.get_location("Bunker 2 - CCTV Tape (Agent)", player),
                 lambda state: (
                     state.has("Watch Magnet (Bunker 2 / Archives Loadout)", player) and
                     state.has("Cell Key 1 (Bunker 2 Item)", player)
                 ))
    if "Bunker 2 - CCTV Tape (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 2 - CCTV Tape (Double Agent)"):
        add_rule(world.multiworld.get_location("Bunker 2 - CCTV Tape (Double Agent)", player),
                 lambda state: (
                     state.has("Watch Magnet (Bunker 2 / Archives Loadout)", player) and
                     state.has("Cell Key 1 (Bunker 2 Item)", player)
                 ))
    if "Bunker 2 - CCTV Tape (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 2 - CCTV Tape (Secret Agent)"):
        add_rule(world.multiworld.get_location("Bunker 2 - CCTV Tape (Secret Agent)", player),
                 lambda state: (
                     state.has("Watch Magnet (Bunker 2 / Archives Loadout)", player) and
                     state.has("Cell Key 1 (Bunker 2 Item)", player)
                 ))
    if "Bunker 2 - Casualty List (Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 2 - Casualty List (Agent)"):
        add_rule(world.multiworld.get_location("Bunker 2 - Casualty List (Agent)", player),
                 lambda state: (
                     state.has("Watch Magnet (Bunker 2 / Archives Loadout)", player) and
                     state.has("Cell Key 1 (Bunker 2 Item)", player) and
                     state.has("Keycard A (Bunker 2 Item)", player)
                 ))
    if "Bunker 2 - Casualty List (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 2 - Casualty List (Double Agent)"):
        add_rule(world.multiworld.get_location("Bunker 2 - Casualty List (Double Agent)", player),
                 lambda state: (
                     state.has("Watch Magnet (Bunker 2 / Archives Loadout)", player) and
                     state.has("Cell Key 1 (Bunker 2 Item)", player) and
                     state.has("Keycard A (Bunker 2 Item)", player)
                 ))
    if "Bunker 2 - Casualty List (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 2 - Casualty List (Secret Agent)"):
        add_rule(world.multiworld.get_location("Bunker 2 - Casualty List (Secret Agent)", player),
                 lambda state: (
                     state.has("Watch Magnet (Bunker 2 / Archives Loadout)", player) and
                     state.has("Cell Key 1 (Bunker 2 Item)", player) and
                     state.has("Keycard A (Bunker 2 Item)", player)
                 ))
    if "Bunker 2 - Cell Key 1 (Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 2 - Cell Key 1 (Agent)"):
        add_rule(world.multiworld.get_location("Bunker 2 - Cell Key 1 (Agent)", player),
                 lambda state: state.has("Watch Magnet (Bunker 2 / Archives Loadout)", player))
    if "Bunker 2 - Cell Key 1 (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 2 - Cell Key 1 (Double Agent)"):
        add_rule(world.multiworld.get_location("Bunker 2 - Cell Key 1 (Double Agent)", player),
                 lambda state: state.has("Watch Magnet (Bunker 2 / Archives Loadout)", player))
    if "Bunker 2 - Cell Key 1 (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 2 - Cell Key 1 (Secret Agent)"):
        add_rule(world.multiworld.get_location("Bunker 2 - Cell Key 1 (Secret Agent)", player),
                 lambda state: state.has("Watch Magnet (Bunker 2 / Archives Loadout)", player))
    if "Bunker 2 - Cell Key 2 (Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 2 - Cell Key 2 (Agent)"):
        add_rule(world.multiworld.get_location("Bunker 2 - Cell Key 2 (Agent)", player),
                 lambda state: (
                     state.has("Watch Magnet (Bunker 2 / Archives Loadout)", player) and
                     state.has("Cell Key 1 (Bunker 2 Item)", player)
                 ))
    if "Bunker 2 - Cell Key 2 (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 2 - Cell Key 2 (Double Agent)"):
        add_rule(world.multiworld.get_location("Bunker 2 - Cell Key 2 (Double Agent)", player),
                 lambda state: (
                     state.has("Watch Magnet (Bunker 2 / Archives Loadout)", player) and
                     state.has("Cell Key 1 (Bunker 2 Item)", player)
                 ))
    if "Bunker 2 - Cell Key 2 (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 2 - Cell Key 2 (Secret Agent)"):
        add_rule(world.multiworld.get_location("Bunker 2 - Cell Key 2 (Secret Agent)", player),
                 lambda state: (
                     state.has("Watch Magnet (Bunker 2 / Archives Loadout)", player) and
                     state.has("Cell Key 1 (Bunker 2 Item)", player)
                 ))
    if "Bunker 2 - Compare Staff/Casualty Lists (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 2 - Compare Staff/Casualty Lists (Double Agent)"):
        add_rule(world.multiworld.get_location("Bunker 2 - Compare Staff/Casualty Lists (Double Agent)", player),
                 lambda state: (
                     state.has("Watch Magnet (Bunker 2 / Archives Loadout)", player) and
                     state.has("Cell Key 1 (Bunker 2 Item)", player) and
                     state.has("Keycard A (Bunker 2 Item)", player) and
                     state.has("Keycard B (Bunker 2 Item)", player) and
                     state.has("Staff List (Bunker 2 Item)", player) and
                     state.has("Casualty List (Bunker 2 Item)", player)
                 ))
    if "Bunker 2 - Compare Staff/Casualty Lists (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 2 - Compare Staff/Casualty Lists (Secret Agent)"):
        add_rule(world.multiworld.get_location("Bunker 2 - Compare Staff/Casualty Lists (Secret Agent)", player),
                 lambda state: (
                     state.has("Watch Magnet (Bunker 2 / Archives Loadout)", player) and
                     state.has("Cell Key 1 (Bunker 2 Item)", player) and
                     state.has("Keycard A (Bunker 2 Item)", player) and
                     state.has("Keycard B (Bunker 2 Item)", player) and
                     state.has("Staff List (Bunker 2 Item)", player) and
                     state.has("Casualty List (Bunker 2 Item)", player)
                 ))
    if "Bunker 2 - Disable All Security Cameras (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 2 - Disable All Security Cameras (Double Agent)"):
        add_rule(world.multiworld.get_location("Bunker 2 - Disable All Security Cameras (Double Agent)", player),
                 lambda state: (
                     state.has("Watch Magnet (Bunker 2 / Archives Loadout)", player) and
                     state.has("Cell Key 1 (Bunker 2 Item)", player) and
                     state.has("Keycard B (Bunker 2 Item)", player)
                 ))
    if "Bunker 2 - Disable All Security Cameras (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 2 - Disable All Security Cameras (Secret Agent)"):
        add_rule(world.multiworld.get_location("Bunker 2 - Disable All Security Cameras (Secret Agent)", player),
                 lambda state: (
                     state.has("Watch Magnet (Bunker 2 / Archives Loadout)", player) and
                     state.has("Cell Key 1 (Bunker 2 Item)", player) and
                     state.has("Keycard B (Bunker 2 Item)", player)
                 ))
    if "Bunker 2 - Double Agent (Clear)" in world.location_name_to_id and is_valid_location(world, "Bunker 2 - Double Agent (Clear)"):
        add_rule(world.multiworld.get_location("Bunker 2 - Double Agent (Clear)", player),
                 lambda state: (
                     state.has("Watch Magnet (Bunker 2 / Archives Loadout)", player) and
                     state.has("Cell Key 1 (Bunker 2 Item)", player) and
                     state.has("Cell Key 2 (Bunker 2 Item)", player) and
                     state.has("CCTV Tape (Bunker 2 Item)", player) and
                     state.has("Keycard A (Bunker 2 Item)", player) and
                     state.has("Keycard B (Bunker 2 Item)", player) and
                     state.has("Safe Key 1 (Bunker 2 Item)", player) and
                     state.has("Safe Key 2 (Bunker 2 Item)", player) and
                     state.has("Staff List (Bunker 2 Item)", player) and
                     state.has("Casualty List (Bunker 2 Item)", player) and
                     state.has("Goldeneye Operations Manual (Bunker 2 Item)", player)
                 ))
    if "Bunker 2 - Escape With Natalya (Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 2 - Escape With Natalya (Agent)"):
        add_rule(world.multiworld.get_location("Bunker 2 - Escape With Natalya (Agent)", player),
                 lambda state: (
                     state.has("Watch Magnet (Bunker 2 / Archives Loadout)", player) and
                     state.has("Cell Key 1 (Bunker 2 Item)", player) and
                     state.has("Cell Key 2 (Bunker 2 Item)", player) and
                     state.has("Keycard B (Bunker 2 Item)", player)
                 ))
    if "Bunker 2 - Escape With Natalya (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 2 - Escape With Natalya (Double Agent)"):
        add_rule(world.multiworld.get_location("Bunker 2 - Escape With Natalya (Double Agent)", player),
                 lambda state: (
                     state.has("Watch Magnet (Bunker 2 / Archives Loadout)", player) and
                     state.has("Cell Key 1 (Bunker 2 Item)", player) and
                     state.has("Cell Key 2 (Bunker 2 Item)", player) and
                     state.has("Keycard B (Bunker 2 Item)", player)
                 ))
    if "Bunker 2 - Escape With Natalya (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 2 - Escape With Natalya (Secret Agent)"):
        add_rule(world.multiworld.get_location("Bunker 2 - Escape With Natalya (Secret Agent)", player),
                 lambda state: (
                     state.has("Watch Magnet (Bunker 2 / Archives Loadout)", player) and
                     state.has("Cell Key 1 (Bunker 2 Item)", player) and
                     state.has("Cell Key 2 (Bunker 2 Item)", player) and
                     state.has("Keycard B (Bunker 2 Item)", player)
                 ))
    if "Bunker 2 - Goldeneye Operations Manual (Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 2 - Goldeneye Operations Manual (Agent)"):
        add_rule(world.multiworld.get_location("Bunker 2 - Goldeneye Operations Manual (Agent)", player),
                 lambda state: (
                     state.has("Watch Magnet (Bunker 2 / Archives Loadout)", player) and
                     state.has("Cell Key 1 (Bunker 2 Item)", player) and
                     state.has("Safe Key 1 (Bunker 2 Item)", player) and
                     state.has("Safe Key 2 (Bunker 2 Item)", player)
                 ))
    if "Bunker 2 - Goldeneye Operations Manual (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 2 - Goldeneye Operations Manual (Double Agent)"):
        add_rule(world.multiworld.get_location("Bunker 2 - Goldeneye Operations Manual (Double Agent)", player),
                 lambda state: (
                     state.has("Watch Magnet (Bunker 2 / Archives Loadout)", player) and
                     state.has("Cell Key 1 (Bunker 2 Item)", player) and
                     state.has("Safe Key 1 (Bunker 2 Item)", player) and
                     state.has("Safe Key 2 (Bunker 2 Item)", player)
                 ))
    if "Bunker 2 - Goldeneye Operations Manual (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 2 - Goldeneye Operations Manual (Secret Agent)"):
        add_rule(world.multiworld.get_location("Bunker 2 - Goldeneye Operations Manual (Secret Agent)", player),
                 lambda state: (
                     state.has("Watch Magnet (Bunker 2 / Archives Loadout)", player) and
                     state.has("Cell Key 1 (Bunker 2 Item)", player) and
                     state.has("Safe Key 1 (Bunker 2 Item)", player) and
                     state.has("Safe Key 2 (Bunker 2 Item)", player)
                 ))
    if "Bunker 2 - Keycard A (Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 2 - Keycard A (Agent)"):
        add_rule(world.multiworld.get_location("Bunker 2 - Keycard A (Agent)", player),
                 lambda state: (
                     state.has("Watch Magnet (Bunker 2 / Archives Loadout)", player) and
                     state.has("Cell Key 1 (Bunker 2 Item)", player)
                 ))
    if "Bunker 2 - Keycard A (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 2 - Keycard A (Double Agent)"):
        add_rule(world.multiworld.get_location("Bunker 2 - Keycard A (Double Agent)", player),
                 lambda state: (
                     state.has("Watch Magnet (Bunker 2 / Archives Loadout)", player) and
                     state.has("Cell Key 1 (Bunker 2 Item)", player)
                 ))
    if "Bunker 2 - Keycard A (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 2 - Keycard A (Secret Agent)"):
        add_rule(world.multiworld.get_location("Bunker 2 - Keycard A (Secret Agent)", player),
                 lambda state: (
                     state.has("Watch Magnet (Bunker 2 / Archives Loadout)", player) and
                     state.has("Cell Key 1 (Bunker 2 Item)", player)
                 ))
    if "Bunker 2 - Keycard B (Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 2 - Keycard B (Agent)"):
        add_rule(world.multiworld.get_location("Bunker 2 - Keycard B (Agent)", player),
                 lambda state: (
                     state.has("Watch Magnet (Bunker 2 / Archives Loadout)", player) and
                     state.has("Cell Key 1 (Bunker 2 Item)", player)
                 ))
    if "Bunker 2 - Keycard B (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 2 - Keycard B (Double Agent)"):
        add_rule(world.multiworld.get_location("Bunker 2 - Keycard B (Double Agent)", player),
                 lambda state: (
                     state.has("Watch Magnet (Bunker 2 / Archives Loadout)", player) and
                     state.has("Cell Key 1 (Bunker 2 Item)", player)
                 ))
    if "Bunker 2 - Keycard B (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 2 - Keycard B (Secret Agent)"):
        add_rule(world.multiworld.get_location("Bunker 2 - Keycard B (Secret Agent)", player),
                 lambda state: (
                     state.has("Watch Magnet (Bunker 2 / Archives Loadout)", player) and
                     state.has("Cell Key 1 (Bunker 2 Item)", player)
                 ))
    if "Bunker 2 - Recover CCTV Tape (Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 2 - Recover CCTV Tape (Agent)"):
        add_rule(world.multiworld.get_location("Bunker 2 - Recover CCTV Tape (Agent)", player),
                 lambda state: (
                     state.has("Watch Magnet (Bunker 2 / Archives Loadout)", player) and
                     state.has("Cell Key 1 (Bunker 2 Item)", player) and
                     state.has("CCTV Tape (Bunker 2 Item)", player)
                 ))
    if "Bunker 2 - Recover CCTV Tape (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 2 - Recover CCTV Tape (Double Agent)"):
        add_rule(world.multiworld.get_location("Bunker 2 - Recover CCTV Tape (Double Agent)", player),
                 lambda state: (
                     state.has("Watch Magnet (Bunker 2 / Archives Loadout)", player) and
                     state.has("Cell Key 1 (Bunker 2 Item)", player) and
                     state.has("CCTV Tape (Bunker 2 Item)", player)
                 ))
    if "Bunker 2 - Recover CCTV Tape (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 2 - Recover CCTV Tape (Secret Agent)"):
        add_rule(world.multiworld.get_location("Bunker 2 - Recover CCTV Tape (Secret Agent)", player),
                 lambda state: (
                     state.has("Watch Magnet (Bunker 2 / Archives Loadout)", player) and
                     state.has("Cell Key 1 (Bunker 2 Item)", player) and
                     state.has("CCTV Tape (Bunker 2 Item)", player)
                 ))
    if "Bunker 2 - Recover Goldeneye Operations Manual (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 2 - Recover Goldeneye Operations Manual (Double Agent)"):
        add_rule(world.multiworld.get_location("Bunker 2 - Recover Goldeneye Operations Manual (Double Agent)", player),
                 lambda state: (
                     state.has("Watch Magnet (Bunker 2 / Archives Loadout)", player) and
                     state.has("Cell Key 1 (Bunker 2 Item)", player) and
                     state.has("Keycard B (Bunker 2 Item)", player) and
                     state.has("Safe Key 1 (Bunker 2 Item)", player) and
                     state.has("Safe Key 2 (Bunker 2 Item)", player) and
                     state.has("Goldeneye Operations Manual (Bunker 2 Item)", player)
                 ))
    if "Bunker 2 - Safe Key 1 (Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 2 - Safe Key 1 (Agent)"):
        add_rule(world.multiworld.get_location("Bunker 2 - Safe Key 1 (Agent)", player),
                 lambda state: (
                     state.has("Watch Magnet (Bunker 2 / Archives Loadout)", player) and
                     state.has("Cell Key 1 (Bunker 2 Item)", player)
                 ))
    if "Bunker 2 - Safe Key 1 (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 2 - Safe Key 1 (Double Agent)"):
        add_rule(world.multiworld.get_location("Bunker 2 - Safe Key 1 (Double Agent)", player),
                 lambda state: (
                     state.has("Watch Magnet (Bunker 2 / Archives Loadout)", player) and
                     state.has("Cell Key 1 (Bunker 2 Item)", player)
                 ))
    if "Bunker 2 - Safe Key 1 (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 2 - Safe Key 1 (Secret Agent)"):
        add_rule(world.multiworld.get_location("Bunker 2 - Safe Key 1 (Secret Agent)", player),
                 lambda state: (
                     state.has("Watch Magnet (Bunker 2 / Archives Loadout)", player) and
                     state.has("Cell Key 1 (Bunker 2 Item)", player)
                 ))
    if "Bunker 2 - Safe Key 2 (Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 2 - Safe Key 2 (Agent)"):
        add_rule(world.multiworld.get_location("Bunker 2 - Safe Key 2 (Agent)", player),
                 lambda state: (
                     state.has("Watch Magnet (Bunker 2 / Archives Loadout)", player) and
                     state.has("Cell Key 1 (Bunker 2 Item)", player)
                 ))
    if "Bunker 2 - Safe Key 2 (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 2 - Safe Key 2 (Double Agent)"):
        add_rule(world.multiworld.get_location("Bunker 2 - Safe Key 2 (Double Agent)", player),
                 lambda state: (
                     state.has("Watch Magnet (Bunker 2 / Archives Loadout)", player) and
                     state.has("Cell Key 1 (Bunker 2 Item)", player)
                 ))
    if "Bunker 2 - Safe Key 2 (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 2 - Safe Key 2 (Secret Agent)"):
        add_rule(world.multiworld.get_location("Bunker 2 - Safe Key 2 (Secret Agent)", player),
                 lambda state: (
                     state.has("Watch Magnet (Bunker 2 / Archives Loadout)", player) and
                     state.has("Cell Key 1 (Bunker 2 Item)", player)
                 ))
    if "Bunker 2 - Secret Agent (Clear)" in world.location_name_to_id and is_valid_location(world, "Bunker 2 - Secret Agent (Clear)"):
        add_rule(world.multiworld.get_location("Bunker 2 - Secret Agent (Clear)", player),
                 lambda state: (
                     state.has("Watch Magnet (Bunker 2 / Archives Loadout)", player) and
                     state.has("Cell Key 1 (Bunker 2 Item)", player) and
                     state.has("Cell Key 2 (Bunker 2 Item)", player) and
                     state.has("CCTV Tape (Bunker 2 Item)", player) and
                     state.has("Keycard A (Bunker 2 Item)", player) and
                     state.has("Keycard B (Bunker 2 Item)", player) and
                     state.has("Staff List (Bunker 2 Item)", player) and
                     state.has("Casualty List (Bunker 2 Item)", player)
                 ))
    if "Bunker 2 - Staff List (Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 2 - Staff List (Agent)"):
        add_rule(world.multiworld.get_location("Bunker 2 - Staff List (Agent)", player),
                 lambda state: (
                     state.has("Watch Magnet (Bunker 2 / Archives Loadout)", player) and
                     state.has("Cell Key 1 (Bunker 2 Item)", player)
                 ))
    if "Bunker 2 - Staff List (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 2 - Staff List (Double Agent)"):
        add_rule(world.multiworld.get_location("Bunker 2 - Staff List (Double Agent)", player),
                 lambda state: (
                     state.has("Watch Magnet (Bunker 2 / Archives Loadout)", player) and
                     state.has("Cell Key 1 (Bunker 2 Item)", player)
                 ))
    if "Bunker 2 - Staff List (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 2 - Staff List (Secret Agent)"):
        add_rule(world.multiworld.get_location("Bunker 2 - Staff List (Secret Agent)", player),
                 lambda state: (
                     state.has("Watch Magnet (Bunker 2 / Archives Loadout)", player) and
                     state.has("Cell Key 1 (Bunker 2 Item)", player)
                 ))
    if "Bunker 2 - Throwing Knives" in world.location_name_to_id and is_valid_location(world, "Bunker 2 - Throwing Knives"):
        add_rule(world.multiworld.get_location("Bunker 2 - Throwing Knives", player),
                 lambda state: (
                     state.has("Watch Magnet (Bunker 2 / Archives Loadout)", player) and
                     state.has("Cell Key 1 (Bunker 2 Item)", player)
                 ))
    if "Bunker 2 - Throwing Knives (Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 2 - Throwing Knives (Agent)"):
        add_rule(world.multiworld.get_location("Bunker 2 - Throwing Knives (Agent)", player),
                 lambda state: (
                     state.has("Watch Magnet (Bunker 2 / Archives Loadout)", player) and
                     state.has("Cell Key 1 (Bunker 2 Item)", player)
                 ))
    if "Bunker 2 - Throwing Knives (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 2 - Throwing Knives (Double Agent)"):
        add_rule(world.multiworld.get_location("Bunker 2 - Throwing Knives (Double Agent)", player),
                 lambda state: (
                     state.has("Watch Magnet (Bunker 2 / Archives Loadout)", player) and
                     state.has("Cell Key 1 (Bunker 2 Item)", player)
                 ))
    if "Bunker 2 - Throwing Knives (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Bunker 2 - Throwing Knives (Secret Agent)"):
        add_rule(world.multiworld.get_location("Bunker 2 - Throwing Knives (Secret Agent)", player),
                 lambda state: (
                     state.has("Watch Magnet (Bunker 2 / Archives Loadout)", player) and
                     state.has("Cell Key 1 (Bunker 2 Item)", player)
                 ))
    if "Caverns - (Clear)" in world.location_name_to_id and is_valid_location(world, "Caverns - (Clear)"):
        add_rule(world.multiworld.get_location("Caverns - (Clear)", player),
                 lambda state: (
                     state.has("Code Card 1 (Caverns Item)", player) and
                     state.has("Code Card 2 (Caverns Item)", player) and
                     state.has("Code Card 3 (Caverns Item)", player) and
                     has_gun(state, player)
                 ))
    if "Caverns - AR33 Assault Rifle" in world.location_name_to_id and is_valid_location(world, "Caverns - AR33 Assault Rifle"):
        add_rule(world.multiworld.get_location("Caverns - AR33 Assault Rifle", player),
                 lambda state: has_gun(state, player))
    if "Caverns - AR33 Assault Rifle (Agent)" in world.location_name_to_id and is_valid_location(world, "Caverns - AR33 Assault Rifle (Agent)"):
        add_rule(world.multiworld.get_location("Caverns - AR33 Assault Rifle (Agent)", player),
                 lambda state: has_gun(state, player))
    if "Caverns - AR33 Assault Rifle (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Caverns - AR33 Assault Rifle (Double Agent)"):
        add_rule(world.multiworld.get_location("Caverns - AR33 Assault Rifle (Double Agent)", player),
                 lambda state: has_gun(state, player))
    if "Caverns - AR33 Assault Rifle (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Caverns - AR33 Assault Rifle (Secret Agent)"):
        add_rule(world.multiworld.get_location("Caverns - AR33 Assault Rifle (Secret Agent)", player),
                 lambda state: has_gun(state, player))
    if "Caverns - Agent (Clear)" in world.location_name_to_id and is_valid_location(world, "Caverns - Agent (Clear)"):
        add_rule(world.multiworld.get_location("Caverns - Agent (Clear)", player),
                 lambda state: (
                     state.has("Code Card 1 (Caverns Item)", player) and
                     state.has("Code Card 2 (Caverns Item)", player) and
                     state.has("Code Card 3 (Caverns Item)", player) and
                     has_gun(state, player)
                 ))
    if "Caverns - Code Card 1 (Agent)" in world.location_name_to_id and is_valid_location(world, "Caverns - Code Card 1 (Agent)"):
        add_rule(world.multiworld.get_location("Caverns - Code Card 1 (Agent)", player),
                 lambda state: has_gun(state, player))
    if "Caverns - Code Card 1 (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Caverns - Code Card 1 (Double Agent)"):
        add_rule(world.multiworld.get_location("Caverns - Code Card 1 (Double Agent)", player),
                 lambda state: has_gun(state, player))
    if "Caverns - Code Card 1 (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Caverns - Code Card 1 (Secret Agent)"):
        add_rule(world.multiworld.get_location("Caverns - Code Card 1 (Secret Agent)", player),
                 lambda state: has_gun(state, player))
    if "Caverns - Code Card 2 (Agent)" in world.location_name_to_id and is_valid_location(world, "Caverns - Code Card 2 (Agent)"):
        add_rule(world.multiworld.get_location("Caverns - Code Card 2 (Agent)", player),
                 lambda state: has_gun(state, player))
    if "Caverns - Code Card 2 (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Caverns - Code Card 2 (Double Agent)"):
        add_rule(world.multiworld.get_location("Caverns - Code Card 2 (Double Agent)", player),
                 lambda state: has_gun(state, player))
    if "Caverns - Code Card 2 (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Caverns - Code Card 2 (Secret Agent)"):
        add_rule(world.multiworld.get_location("Caverns - Code Card 2 (Secret Agent)", player),
                 lambda state: has_gun(state, player))
    if "Caverns - Code Card 3 (Agent)" in world.location_name_to_id and is_valid_location(world, "Caverns - Code Card 3 (Agent)"):
        add_rule(world.multiworld.get_location("Caverns - Code Card 3 (Agent)", player),
                 lambda state: has_gun(state, player))
    if "Caverns - Code Card 3 (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Caverns - Code Card 3 (Double Agent)"):
        add_rule(world.multiworld.get_location("Caverns - Code Card 3 (Double Agent)", player),
                 lambda state: has_gun(state, player))
    if "Caverns - Code Card 3 (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Caverns - Code Card 3 (Secret Agent)"):
        add_rule(world.multiworld.get_location("Caverns - Code Card 3 (Secret Agent)", player),
                 lambda state: has_gun(state, player))
    if "Caverns - Destroy Inlet Pump Controls" in world.location_name_to_id and is_valid_location(world, "Caverns - Destroy Inlet Pump Controls"):
        add_rule(world.multiworld.get_location("Caverns - Destroy Inlet Pump Controls", player),
                 lambda state: has_gun(state, player))
    if "Caverns - Destroy Inlet Pump Controls (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Caverns - Destroy Inlet Pump Controls (Double Agent)"):
        add_rule(world.multiworld.get_location("Caverns - Destroy Inlet Pump Controls (Double Agent)", player),
                 lambda state: has_gun(state, player))
    if "Caverns - Destroy Inlet Pump Controls (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Caverns - Destroy Inlet Pump Controls (Secret Agent)"):
        add_rule(world.multiworld.get_location("Caverns - Destroy Inlet Pump Controls (Secret Agent)", player),
                 lambda state: has_gun(state, player))
    if "Caverns - Destroy Master Control Console" in world.location_name_to_id and is_valid_location(world, "Caverns - Destroy Master Control Console"):
        add_rule(world.multiworld.get_location("Caverns - Destroy Master Control Console", player),
                 lambda state: has_gun(state, player))
    if "Caverns - Destroy Master Control Console (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Caverns - Destroy Master Control Console (Double Agent)"):
        add_rule(world.multiworld.get_location("Caverns - Destroy Master Control Console (Double Agent)", player),
                 lambda state: has_gun(state, player))
    if "Caverns - Destroy Master Control Console (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Caverns - Destroy Master Control Console (Secret Agent)"):
        add_rule(world.multiworld.get_location("Caverns - Destroy Master Control Console (Secret Agent)", player),
                 lambda state: has_gun(state, player))
    if "Caverns - Destroy Outlet Pump controls" in world.location_name_to_id and is_valid_location(world, "Caverns - Destroy Outlet Pump controls"):
        add_rule(world.multiworld.get_location("Caverns - Destroy Outlet Pump controls", player),
                 lambda state: has_gun(state, player))
    if "Caverns - Destroy Outlet Pump controls (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Caverns - Destroy Outlet Pump controls (Double Agent)"):
        add_rule(world.multiworld.get_location("Caverns - Destroy Outlet Pump controls (Double Agent)", player),
                 lambda state: has_gun(state, player))
    if "Caverns - Destroy Outlet Pump controls (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Caverns - Destroy Outlet Pump controls (Secret Agent)"):
        add_rule(world.multiworld.get_location("Caverns - Destroy Outlet Pump controls (Secret Agent)", player),
                 lambda state: has_gun(state, player))
    if "Caverns - Double Agent (Clear)" in world.location_name_to_id and is_valid_location(world, "Caverns - Double Agent (Clear)"):
        add_rule(world.multiworld.get_location("Caverns - Double Agent (Clear)", player),
                 lambda state: (
                     state.has("Code Card 1 (Caverns Item)", player) and
                     state.has("Code Card 2 (Caverns Item)", player) and
                     state.has("Code Card 3 (Caverns Item)", player) and
                     has_gun(state, player)
                 ))
    if "Caverns - Minimize Caverns Scientist Casualties" in world.location_name_to_id and is_valid_location(world, "Caverns - Minimize Caverns Scientist Casualties"):
        add_rule(world.multiworld.get_location("Caverns - Minimize Caverns Scientist Casualties", player),
                 lambda state: has_gun(state, player))
    if "Caverns - Minimize Caverns Scientist Casualties (Agent)" in world.location_name_to_id and is_valid_location(world, "Caverns - Minimize Caverns Scientist Casualties (Agent)"):
        add_rule(world.multiworld.get_location("Caverns - Minimize Caverns Scientist Casualties (Agent)", player),
                 lambda state: (
                     state.has("Code Card 1 (Caverns Item)", player) and
                     state.has("Code Card 2 (Caverns Item)", player) and
                     state.has("Code Card 3 (Caverns Item)", player) and
                     has_gun(state, player)
                 ))
    if "Caverns - Minimize Caverns Scientist Casualties (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Caverns - Minimize Caverns Scientist Casualties (Double Agent)"):
        add_rule(world.multiworld.get_location("Caverns - Minimize Caverns Scientist Casualties (Double Agent)", player),
                 lambda state: (
                     state.has("Code Card 1 (Caverns Item)", player) and
                     state.has("Code Card 2 (Caverns Item)", player) and
                     state.has("Code Card 3 (Caverns Item)", player) and
                     has_gun(state, player)
                 ))
    if "Caverns - Minimize Caverns Scientist Casualties (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Caverns - Minimize Caverns Scientist Casualties (Secret Agent)"):
        add_rule(world.multiworld.get_location("Caverns - Minimize Caverns Scientist Casualties (Secret Agent)", player),
                 lambda state: (
                     state.has("Code Card 1 (Caverns Item)", player) and
                     state.has("Code Card 2 (Caverns Item)", player) and
                     state.has("Code Card 3 (Caverns Item)", player) and
                     has_gun(state, player)
                 ))
    if "Caverns - Secret Agent (Clear)" in world.location_name_to_id and is_valid_location(world, "Caverns - Secret Agent (Clear)"):
        add_rule(world.multiworld.get_location("Caverns - Secret Agent (Clear)", player),
                 lambda state: (
                     state.has("Code Card 1 (Caverns Item)", player) and
                     state.has("Code Card 2 (Caverns Item)", player) and
                     state.has("Code Card 3 (Caverns Item)", player) and
                     has_gun(state, player)
                 ))
    if "Caverns - Use Radio To Contact Jack Wade" in world.location_name_to_id and is_valid_location(world, "Caverns - Use Radio To Contact Jack Wade"):
        add_rule(world.multiworld.get_location("Caverns - Use Radio To Contact Jack Wade", player),
                 lambda state: has_gun(state, player))
    if "Caverns - Use Radio To Contact Jack Wade (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Caverns - Use Radio To Contact Jack Wade (Double Agent)"):
        add_rule(world.multiworld.get_location("Caverns - Use Radio To Contact Jack Wade (Double Agent)", player),
                 lambda state: has_gun(state, player))
    if "Control - (Clear)" in world.location_name_to_id and is_valid_location(world, "Control - (Clear)"):
        add_rule(world.multiworld.get_location("Control - (Clear)", player),
                 lambda state: (
                     has_gun(state, player) and
                     has_explosive(state, player)
                 ))
    if "Control - Agent (Clear)" in world.location_name_to_id and is_valid_location(world, "Control - Agent (Clear)"):
        add_rule(world.multiworld.get_location("Control - Agent (Clear)", player),
                 lambda state: (
                     has_gun(state, player) and
                     has_explosive(state, player)
                 ))
    if "Control - Destroy Armored Mainframes" in world.location_name_to_id and is_valid_location(world, "Control - Destroy Armored Mainframes"):
        add_rule(world.multiworld.get_location("Control - Destroy Armored Mainframes", player),
                 lambda state: (
                     has_gun(state, player) and
                     has_explosive(state, player)
                 ))
    if "Control - Destroy Armored Mainframes (Agent)" in world.location_name_to_id and is_valid_location(world, "Control - Destroy Armored Mainframes (Agent)"):
        add_rule(world.multiworld.get_location("Control - Destroy Armored Mainframes (Agent)", player),
                 lambda state: (
                     has_gun(state, player) and
                     has_explosive(state, player)
                 ))
    if "Control - Destroy Armored Mainframes (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Control - Destroy Armored Mainframes (Double Agent)"):
        add_rule(world.multiworld.get_location("Control - Destroy Armored Mainframes (Double Agent)", player),
                 lambda state: (
                     has_gun(state, player) and
                     has_explosive(state, player)
                 ))
    if "Control - Destroy Armored Mainframes (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Control - Destroy Armored Mainframes (Secret Agent)"):
        add_rule(world.multiworld.get_location("Control - Destroy Armored Mainframes (Secret Agent)", player),
                 lambda state: (
                     has_gun(state, player) and
                     has_explosive(state, player)
                 ))
    if "Control - Disable Goldeneye Satellite" in world.location_name_to_id and is_valid_location(world, "Control - Disable Goldeneye Satellite"):
        add_rule(world.multiworld.get_location("Control - Disable Goldeneye Satellite", player),
                 lambda state: has_gun(state, player))
    if "Control - Disable Goldeneye Satellite (Agent)" in world.location_name_to_id and is_valid_location(world, "Control - Disable Goldeneye Satellite (Agent)"):
        add_rule(world.multiworld.get_location("Control - Disable Goldeneye Satellite (Agent)", player),
                 lambda state: has_gun(state, player))
    if "Control - Disable Goldeneye Satellite (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Control - Disable Goldeneye Satellite (Double Agent)"):
        add_rule(world.multiworld.get_location("Control - Disable Goldeneye Satellite (Double Agent)", player),
                 lambda state: has_gun(state, player))
    if "Control - Disable Goldeneye Satellite (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Control - Disable Goldeneye Satellite (Secret Agent)"):
        add_rule(world.multiworld.get_location("Control - Disable Goldeneye Satellite (Secret Agent)", player),
                 lambda state: has_gun(state, player))
    if "Control - Double Agent (Clear)" in world.location_name_to_id and is_valid_location(world, "Control - Double Agent (Clear)"):
        add_rule(world.multiworld.get_location("Control - Double Agent (Clear)", player),
                 lambda state: (
                     has_gun(state, player) and
                     has_explosive(state, player)
                 ))
    if "Control - Protect Natalya" in world.location_name_to_id and is_valid_location(world, "Control - Protect Natalya"):
        add_rule(world.multiworld.get_location("Control - Protect Natalya", player),
                 lambda state: has_gun(state, player))
    if "Control - Protect Natalya (Agent)" in world.location_name_to_id and is_valid_location(world, "Control - Protect Natalya (Agent)"):
        add_rule(world.multiworld.get_location("Control - Protect Natalya (Agent)", player),
                 lambda state: has_gun(state, player))
    if "Control - Protect Natalya (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Control - Protect Natalya (Double Agent)"):
        add_rule(world.multiworld.get_location("Control - Protect Natalya (Double Agent)", player),
                 lambda state: has_gun(state, player))
    if "Control - Protect Natalya (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Control - Protect Natalya (Secret Agent)"):
        add_rule(world.multiworld.get_location("Control - Protect Natalya (Secret Agent)", player),
                 lambda state: has_gun(state, player))
    if "Control - Secret Agent (Clear)" in world.location_name_to_id and is_valid_location(world, "Control - Secret Agent (Clear)"):
        add_rule(world.multiworld.get_location("Control - Secret Agent (Clear)", player),
                 lambda state: (
                     has_gun(state, player) and
                     has_explosive(state, player)
                 ))
    if "Cradle - (Clear)" in world.location_name_to_id and is_valid_location(world, "Cradle - (Clear)"):
        add_rule(world.multiworld.get_location("Cradle - (Clear)", player),
                 lambda state: has_gun(state, player))
    if "Cradle - Agent (Clear)" in world.location_name_to_id and is_valid_location(world, "Cradle - Agent (Clear)"):
        add_rule(world.multiworld.get_location("Cradle - Agent (Clear)", player),
                 lambda state: has_gun(state, player))
    if "Cradle - Destroy Control Console" in world.location_name_to_id and is_valid_location(world, "Cradle - Destroy Control Console"):
        add_rule(world.multiworld.get_location("Cradle - Destroy Control Console", player),
                 lambda state: has_gun(state, player))
    if "Cradle - Destroy Control Console (Agent)" in world.location_name_to_id and is_valid_location(world, "Cradle - Destroy Control Console (Agent)"):
        add_rule(world.multiworld.get_location("Cradle - Destroy Control Console (Agent)", player),
                 lambda state: has_gun(state, player))
    if "Cradle - Destroy Control Console (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Cradle - Destroy Control Console (Double Agent)"):
        add_rule(world.multiworld.get_location("Cradle - Destroy Control Console (Double Agent)", player),
                 lambda state: has_gun(state, player))
    if "Cradle - Destroy Control Console (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Cradle - Destroy Control Console (Secret Agent)"):
        add_rule(world.multiworld.get_location("Cradle - Destroy Control Console (Secret Agent)", player),
                 lambda state: has_gun(state, player))
    if "Cradle - Double Agent (Clear)" in world.location_name_to_id and is_valid_location(world, "Cradle - Double Agent (Clear)"):
        add_rule(world.multiworld.get_location("Cradle - Double Agent (Clear)", player),
                 lambda state: has_gun(state, player))
    if "Cradle - Secret Agent (Clear)" in world.location_name_to_id and is_valid_location(world, "Cradle - Secret Agent (Clear)"):
        add_rule(world.multiworld.get_location("Cradle - Secret Agent (Clear)", player),
                 lambda state: has_gun(state, player))
    if "Cradle - Settle The Score With Trevelyan" in world.location_name_to_id and is_valid_location(world, "Cradle - Settle The Score With Trevelyan"):
        add_rule(world.multiworld.get_location("Cradle - Settle The Score With Trevelyan", player),
                 lambda state: has_gun(state, player))
    if "Cradle - Settle The Score With Trevelyan (Agent)" in world.location_name_to_id and is_valid_location(world, "Cradle - Settle The Score With Trevelyan (Agent)"):
        add_rule(world.multiworld.get_location("Cradle - Settle The Score With Trevelyan (Agent)", player),
                 lambda state: has_gun(state, player))
    if "Cradle - Settle The Score With Trevelyan (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Cradle - Settle The Score With Trevelyan (Double Agent)"):
        add_rule(world.multiworld.get_location("Cradle - Settle The Score With Trevelyan (Double Agent)", player),
                 lambda state: has_gun(state, player))
    if "Cradle - Settle The Score With Trevelyan (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Cradle - Settle The Score With Trevelyan (Secret Agent)"):
        add_rule(world.multiworld.get_location("Cradle - Settle The Score With Trevelyan (Secret Agent)", player),
                 lambda state: has_gun(state, player))
    if "Dam - (Clear)" in world.location_name_to_id and is_valid_location(world, "Dam - (Clear)"):
        add_rule(world.multiworld.get_location("Dam - (Clear)", player),
                 lambda state: has_gun(state, player))
    if "Dam - Agent (Clear)" in world.location_name_to_id and is_valid_location(world, "Dam - Agent (Clear)"):
        add_rule(world.multiworld.get_location("Dam - Agent (Clear)", player),
                 lambda state: has_gun(state, player))
    if "Dam - Bungee Jump From Platform" in world.location_name_to_id and is_valid_location(world, "Dam - Bungee Jump From Platform"):
        add_rule(world.multiworld.get_location("Dam - Bungee Jump From Platform", player),
                 lambda state: has_gun(state, player))
    if "Dam - Bungee Jump From Platform (Agent)" in world.location_name_to_id and is_valid_location(world, "Dam - Bungee Jump From Platform (Agent)"):
        add_rule(world.multiworld.get_location("Dam - Bungee Jump From Platform (Agent)", player),
                 lambda state: has_gun(state, player))
    if "Dam - Bungee Jump From Platform (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Dam - Bungee Jump From Platform (Double Agent)"):
        add_rule(world.multiworld.get_location("Dam - Bungee Jump From Platform (Double Agent)", player),
                 lambda state: has_gun(state, player))
    if "Dam - Bungee Jump From Platform (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Dam - Bungee Jump From Platform (Secret Agent)"):
        add_rule(world.multiworld.get_location("Dam - Bungee Jump From Platform (Secret Agent)", player),
                 lambda state: has_gun(state, player))
    if "Dam - Double Agent (Clear)" in world.location_name_to_id and is_valid_location(world, "Dam - Double Agent (Clear)"):
        add_rule(world.multiworld.get_location("Dam - Double Agent (Clear)", player),
                 lambda state: (
                     state.has("Covert Modem (Dam Loadout)", player) and
                     has_gun(state, player)
                 ))
    if "Dam - Install Covert Modem" in world.location_name_to_id and is_valid_location(world, "Dam - Install Covert Modem"):
        add_rule(world.multiworld.get_location("Dam - Install Covert Modem", player),
                 lambda state: (
                     state.has("Covert Modem (Dam Loadout)", player) and
                     has_gun(state, player)
                 ))
    if "Dam - Install Covert Modem (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Dam - Install Covert Modem (Double Agent)"):
        add_rule(world.multiworld.get_location("Dam - Install Covert Modem (Double Agent)", player),
                 lambda state: (
                     state.has("Covert Modem (Dam Loadout)", player) and
                     has_gun(state, player)
                 ))
    if "Dam - Intercept Data Backup" in world.location_name_to_id and is_valid_location(world, "Dam - Intercept Data Backup"):
        add_rule(world.multiworld.get_location("Dam - Intercept Data Backup", player),
                 lambda state: (
                     state.has("Covert Modem (Dam Loadout)", player) and
                     has_gun(state, player)
                 ))
    if "Dam - Intercept Data Backup (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Dam - Intercept Data Backup (Double Agent)"):
        add_rule(world.multiworld.get_location("Dam - Intercept Data Backup (Double Agent)", player),
                 lambda state: (
                     state.has("Covert Modem (Dam Loadout)", player) and
                     has_gun(state, player)
                 ))
    if "Dam - Neutralize All Alarms" in world.location_name_to_id and is_valid_location(world, "Dam - Neutralize All Alarms"):
        add_rule(world.multiworld.get_location("Dam - Neutralize All Alarms", player),
                 lambda state: has_gun(state, player))
    if "Dam - Neutralize All Alarms (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Dam - Neutralize All Alarms (Double Agent)"):
        add_rule(world.multiworld.get_location("Dam - Neutralize All Alarms (Double Agent)", player),
                 lambda state: has_gun(state, player))
    if "Dam - Neutralize All Alarms (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Dam - Neutralize All Alarms (Secret Agent)"):
        add_rule(world.multiworld.get_location("Dam - Neutralize All Alarms (Secret Agent)", player),
                 lambda state: has_gun(state, player))
    if "Dam - Secret Agent (Clear)" in world.location_name_to_id and is_valid_location(world, "Dam - Secret Agent (Clear)"):
        add_rule(world.multiworld.get_location("Dam - Secret Agent (Clear)", player),
                 lambda state: has_gun(state, player))
    if "Depot - Blueprints (Agent)" in world.location_name_to_id and is_valid_location(world, "Depot - Blueprints (Agent)"):
        add_rule(world.multiworld.get_location("Depot - Blueprints (Agent)", player),
                 lambda state: state.has("Depot Safe Key (Depot Item)", player))
    if "Depot - Blueprints (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Depot - Blueprints (Double Agent)"):
        add_rule(world.multiworld.get_location("Depot - Blueprints (Double Agent)", player),
                 lambda state: state.has("Depot Safe Key (Depot Item)", player))
    if "Depot - Blueprints (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Depot - Blueprints (Secret Agent)"):
        add_rule(world.multiworld.get_location("Depot - Blueprints (Secret Agent)", player),
                 lambda state: state.has("Depot Safe Key (Depot Item)", player))
    if "Depot - Destroy Computer Network" in world.location_name_to_id and is_valid_location(world, "Depot - Destroy Computer Network"):
        add_rule(world.multiworld.get_location("Depot - Destroy Computer Network", player),
                 lambda state: has_gun(state, player))
    if "Depot - Destroy Computer Network (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Depot - Destroy Computer Network (Double Agent)"):
        add_rule(world.multiworld.get_location("Depot - Destroy Computer Network (Double Agent)", player),
                 lambda state: has_gun(state, player))
    if "Depot - Destroy Computer Network (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Depot - Destroy Computer Network (Secret Agent)"):
        add_rule(world.multiworld.get_location("Depot - Destroy Computer Network (Secret Agent)", player),
                 lambda state: has_gun(state, player))
    if "Depot - Destroy Illegal Arms Cache" in world.location_name_to_id and is_valid_location(world, "Depot - Destroy Illegal Arms Cache"):
        add_rule(world.multiworld.get_location("Depot - Destroy Illegal Arms Cache", player),
                 lambda state: has_gun(state, player))
    if "Depot - Destroy Illegal Arms Cache (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Depot - Destroy Illegal Arms Cache (Double Agent)"):
        add_rule(world.multiworld.get_location("Depot - Destroy Illegal Arms Cache (Double Agent)", player),
                 lambda state: has_gun(state, player))
    if "Depot - Double Agent (Clear)" in world.location_name_to_id and is_valid_location(world, "Depot - Double Agent (Clear)"):
        add_rule(world.multiworld.get_location("Depot - Double Agent (Clear)", player),
                 lambda state: (
                     state.has("Depot Safe Key (Depot Item)", player) and
                     state.has("Blueprints (Depot Item)", player) and
                     has_gun(state, player)
                 ))
    if "Depot - Locate Trevelyn's Train (Agent)" in world.location_name_to_id and is_valid_location(world, "Depot - Locate Trevelyn's Train (Agent)"):
        add_rule(world.multiworld.get_location("Depot - Locate Trevelyn's Train (Agent)", player),
                 lambda state: has_gun(state, player))
    if "Depot - Obtain Depot Safe Key" in world.location_name_to_id and is_valid_location(world, "Depot - Obtain Depot Safe Key"):
        add_rule(world.multiworld.get_location("Depot - Obtain Depot Safe Key", player),
                 lambda state: state.has("Depot Safe Key (Depot Item)", player))
    if "Depot - Obtain Depot Safe Key (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Depot - Obtain Depot Safe Key (Double Agent)"):
        add_rule(world.multiworld.get_location("Depot - Obtain Depot Safe Key (Double Agent)", player),
                 lambda state: state.has("Depot Safe Key (Depot Item)", player))
    if "Depot - Obtain Depot Safe Key (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Depot - Obtain Depot Safe Key (Secret Agent)"):
        add_rule(world.multiworld.get_location("Depot - Obtain Depot Safe Key (Secret Agent)", player),
                 lambda state: state.has("Depot Safe Key (Depot Item)", player))
    if "Depot - Recover Helicopter Blueprints" in world.location_name_to_id and is_valid_location(world, "Depot - Recover Helicopter Blueprints"):
        add_rule(world.multiworld.get_location("Depot - Recover Helicopter Blueprints", player),
                 lambda state: (
                     state.has("Depot Safe Key (Depot Item)", player) and
                     state.has("Blueprints (Depot Item)", player)
                 ))
    if "Depot - Recover Helicopter Blueprints (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Depot - Recover Helicopter Blueprints (Double Agent)"):
        add_rule(world.multiworld.get_location("Depot - Recover Helicopter Blueprints (Double Agent)", player),
                 lambda state: (
                     state.has("Depot Safe Key (Depot Item)", player) and
                     state.has("Blueprints (Depot Item)", player)
                 ))
    if "Depot - Recover Helicopter Blueprints (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Depot - Recover Helicopter Blueprints (Secret Agent)"):
        add_rule(world.multiworld.get_location("Depot - Recover Helicopter Blueprints (Secret Agent)", player),
                 lambda state: (
                     state.has("Depot Safe Key (Depot Item)", player) and
                     state.has("Blueprints (Depot Item)", player)
                 ))
    if "Depot - Secret Agent (Clear)" in world.location_name_to_id and is_valid_location(world, "Depot - Secret Agent (Clear)"):
        add_rule(world.multiworld.get_location("Depot - Secret Agent (Clear)", player),
                 lambda state: (
                     state.has("Depot Safe Key (Depot Item)", player) and
                     state.has("Blueprints (Depot Item)", player) and
                     has_gun(state, player)
                 ))
    if "Egyptian - (Clear)" in world.location_name_to_id and is_valid_location(world, "Egyptian - (Clear)"):
        add_rule(world.multiworld.get_location("Egyptian - (Clear)", player),
                 lambda state: (
                     state.has("Golden Gun", player) and
                     has_gun(state, player)
                 ))
    if "Egyptian - Agent (Clear)" in world.location_name_to_id and is_valid_location(world, "Egyptian - Agent (Clear)"):
        add_rule(world.multiworld.get_location("Egyptian - Agent (Clear)", player),
                 lambda state: (
                     state.has("Golden Gun", player) and
                     has_gun(state, player)
                 ))
    if "Egyptian - Defeat Baron Samedi?" in world.location_name_to_id and is_valid_location(world, "Egyptian - Defeat Baron Samedi?"):
        add_rule(world.multiworld.get_location("Egyptian - Defeat Baron Samedi?", player),
                 lambda state: has_gun(state, player))
    if "Egyptian - Defeat Baron Samedi? (Agent)" in world.location_name_to_id and is_valid_location(world, "Egyptian - Defeat Baron Samedi? (Agent)"):
        add_rule(world.multiworld.get_location("Egyptian - Defeat Baron Samedi? (Agent)", player),
                 lambda state: (
                     state.has("Golden Gun", player) and
                     has_gun(state, player)
                 ))
    if "Egyptian - Defeat Baron Samedi? (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Egyptian - Defeat Baron Samedi? (Double Agent)"):
        add_rule(world.multiworld.get_location("Egyptian - Defeat Baron Samedi? (Double Agent)", player),
                 lambda state: (
                     state.has("Golden Gun", player) and
                     has_gun(state, player)
                 ))
    if "Egyptian - Defeat Baron Samedi? (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Egyptian - Defeat Baron Samedi? (Secret Agent)"):
        add_rule(world.multiworld.get_location("Egyptian - Defeat Baron Samedi? (Secret Agent)", player),
                 lambda state: (
                     state.has("Golden Gun", player) and
                     has_gun(state, player)
                 ))
    if "Egyptian - Double Agent (Clear)" in world.location_name_to_id and is_valid_location(world, "Egyptian - Double Agent (Clear)"):
        add_rule(world.multiworld.get_location("Egyptian - Double Agent (Clear)", player),
                 lambda state: (
                     state.has("Golden Gun", player) and
                     has_gun(state, player)
                 ))
    if "Egyptian - Golden Gun" in world.location_name_to_id and is_valid_location(world, "Egyptian - Golden Gun"):
        add_rule(world.multiworld.get_location("Egyptian - Golden Gun", player),
                 lambda state: has_gun(state, player))
    if "Egyptian - Golden Gun (Agent)" in world.location_name_to_id and is_valid_location(world, "Egyptian - Golden Gun (Agent)"):
        add_rule(world.multiworld.get_location("Egyptian - Golden Gun (Agent)", player),
                 lambda state: has_gun(state, player))
    if "Egyptian - Golden Gun (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Egyptian - Golden Gun (Double Agent)"):
        add_rule(world.multiworld.get_location("Egyptian - Golden Gun (Double Agent)", player),
                 lambda state: has_gun(state, player))
    if "Egyptian - Golden Gun (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Egyptian - Golden Gun (Secret Agent)"):
        add_rule(world.multiworld.get_location("Egyptian - Golden Gun (Secret Agent)", player),
                 lambda state: has_gun(state, player))
    if "Egyptian - Recover the Golden Gun" in world.location_name_to_id and is_valid_location(world, "Egyptian - Recover the Golden Gun"):
        add_rule(world.multiworld.get_location("Egyptian - Recover the Golden Gun", player),
                 lambda state: has_gun(state, player))
    if "Egyptian - Recover the Golden Gun (Agent)" in world.location_name_to_id and is_valid_location(world, "Egyptian - Recover the Golden Gun (Agent)"):
        add_rule(world.multiworld.get_location("Egyptian - Recover the Golden Gun (Agent)", player),
                 lambda state: (
                     state.has("Golden Gun", player) and
                     has_gun(state, player)
                 ))
    if "Egyptian - Recover the Golden Gun (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Egyptian - Recover the Golden Gun (Double Agent)"):
        add_rule(world.multiworld.get_location("Egyptian - Recover the Golden Gun (Double Agent)", player),
                 lambda state: (
                     state.has("Golden Gun", player) and
                     has_gun(state, player)
                 ))
    if "Egyptian - Recover the Golden Gun (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Egyptian - Recover the Golden Gun (Secret Agent)"):
        add_rule(world.multiworld.get_location("Egyptian - Recover the Golden Gun (Secret Agent)", player),
                 lambda state: (
                     state.has("Golden Gun", player) and
                     has_gun(state, player)
                 ))
    if "Egyptian - Secret Agent (Clear)" in world.location_name_to_id and is_valid_location(world, "Egyptian - Secret Agent (Clear)"):
        add_rule(world.multiworld.get_location("Egyptian - Secret Agent (Clear)", player),
                 lambda state: (
                     state.has("Golden Gun", player) and
                     has_gun(state, player)
                 ))
    if "Facility - (Clear)" in world.location_name_to_id and is_valid_location(world, "Facility - (Clear)"):
        add_rule(world.multiworld.get_location("Facility - (Clear)", player),
                 lambda state: (
                     (state.has("Keycard A (Facility Item)", player) or state.has("Keycard B (Facility Item)", player)) and
                     has_explosive(state, player)
                 ))
    if "Facility - Agent (Clear)" in world.location_name_to_id and is_valid_location(world, "Facility - Agent (Clear)"):
        add_rule(world.multiworld.get_location("Facility - Agent (Clear)", player),
                 lambda state: (
                     (state.has("Keycard A (Facility Item)", player) or state.has("Keycard B (Facility Item)", player)) and
                     has_explosive(state, player)
                 ))
    if "Facility - Contact Double Agent" in world.location_name_to_id and is_valid_location(world, "Facility - Contact Double Agent"):
        add_rule(world.multiworld.get_location("Facility - Contact Double Agent", player),
                 lambda state: (state.has("Keycard A (Facility Item)", player) or state.has("Keycard B (Facility Item)", player)))
    if "Facility - Contact Double Agent (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Facility - Contact Double Agent (Double Agent)"):
        add_rule(world.multiworld.get_location("Facility - Contact Double Agent (Double Agent)", player),
                 lambda state: (state.has("Keycard A (Facility Item)", player) or state.has("Keycard B (Facility Item)", player)))
    if "Facility - Contact Double Agent (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Facility - Contact Double Agent (Secret Agent)"):
        add_rule(world.multiworld.get_location("Facility - Contact Double Agent (Secret Agent)", player),
                 lambda state: (state.has("Keycard A (Facility Item)", player) or state.has("Keycard B (Facility Item)", player)))
    if "Facility - Destroy All Tanks In Bottling Room" in world.location_name_to_id and is_valid_location(world, "Facility - Destroy All Tanks In Bottling Room"):
        add_rule(world.multiworld.get_location("Facility - Destroy All Tanks In Bottling Room", player),
                 lambda state: (
                     (state.has("Keycard A (Facility Item)", player) or state.has("Keycard B (Facility Item)", player)) and
                     has_explosive(state, player)
                 ))
    if "Facility - Destroy All Tanks In Bottling Room (Agent)" in world.location_name_to_id and is_valid_location(world, "Facility - Destroy All Tanks In Bottling Room (Agent)"):
        add_rule(world.multiworld.get_location("Facility - Destroy All Tanks In Bottling Room (Agent)", player),
                 lambda state: (
                     (state.has("Keycard A (Facility Item)", player) or state.has("Keycard B (Facility Item)", player)) and
                     has_explosive(state, player)
                 ))
    if "Facility - Destroy All Tanks In Bottling Room (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Facility - Destroy All Tanks In Bottling Room (Double Agent)"):
        add_rule(world.multiworld.get_location("Facility - Destroy All Tanks In Bottling Room (Double Agent)", player),
                 lambda state: (
                     (state.has("Keycard A (Facility Item)", player) or state.has("Keycard B (Facility Item)", player)) and
                     has_explosive(state, player)
                 ))
    if "Facility - Destroy All Tanks In Bottling Room (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Facility - Destroy All Tanks In Bottling Room (Secret Agent)"):
        add_rule(world.multiworld.get_location("Facility - Destroy All Tanks In Bottling Room (Secret Agent)", player),
                 lambda state: (
                     state.has("Door Decoder (Facility Item)", player) and
                     (state.has("Keycard A (Facility Item)", player) or state.has("Keycard B (Facility Item)", player))
                 ))
    if "Facility - Double Agent (Clear)" in world.location_name_to_id and is_valid_location(world, "Facility - Double Agent (Clear)"):
        add_rule(world.multiworld.get_location("Facility - Double Agent (Clear)", player),
                 lambda state: (
                     state.has("Door Decoder (Facility Item)", player) and
                     (state.has("Keycard A (Facility Item)", player) or state.has("Keycard B (Facility Item)", player))
                 ))
    if "Facility - Gain Entry To Laboratory Area" in world.location_name_to_id and is_valid_location(world, "Facility - Gain Entry To Laboratory Area"):
        add_rule(world.multiworld.get_location("Facility - Gain Entry To Laboratory Area", player),
                 lambda state: (state.has("Keycard A (Facility Item)", player) or state.has("Keycard B (Facility Item)", player)))
    if "Facility - Gain Entry To Laboratory Area (Agent)" in world.location_name_to_id and is_valid_location(world, "Facility - Gain Entry To Laboratory Area (Agent)"):
        add_rule(world.multiworld.get_location("Facility - Gain Entry To Laboratory Area (Agent)", player),
                 lambda state: (state.has("Keycard A (Facility Item)", player) or state.has("Keycard B (Facility Item)", player)))
    if "Facility - Gain Entry To Laboratory Area (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Facility - Gain Entry To Laboratory Area (Double Agent)"):
        add_rule(world.multiworld.get_location("Facility - Gain Entry To Laboratory Area (Double Agent)", player),
                 lambda state: (state.has("Keycard A (Facility Item)", player) or state.has("Keycard B (Facility Item)", player)))
    if "Facility - Gain Entry To Laboratory Area (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Facility - Gain Entry To Laboratory Area (Secret Agent)"):
        add_rule(world.multiworld.get_location("Facility - Gain Entry To Laboratory Area (Secret Agent)", player),
                 lambda state: (state.has("Keycard A (Facility Item)", player) or state.has("Keycard B (Facility Item)", player)))
    if "Facility - Keycard A (Agent)" in world.location_name_to_id and is_valid_location(world, "Facility - Keycard A (Agent)"):
        add_rule(world.multiworld.get_location("Facility - Keycard A (Agent)", player),
                 lambda state: state.has("Keycard B (Facility Item)", player))
    if "Facility - Keycard A (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Facility - Keycard A (Double Agent)"):
        add_rule(world.multiworld.get_location("Facility - Keycard A (Double Agent)", player),
                 lambda state: state.has("Keycard B (Facility Item)", player))
    if "Facility - Keycard A (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Facility - Keycard A (Secret Agent)"):
        add_rule(world.multiworld.get_location("Facility - Keycard A (Secret Agent)", player),
                 lambda state: state.has("Keycard B (Facility Item)", player))
    if "Facility - Minimize Facility Scientist Casualties" in world.location_name_to_id and is_valid_location(world, "Facility - Minimize Facility Scientist Casualties"):
        add_rule(world.multiworld.get_location("Facility - Minimize Facility Scientist Casualties", player),
                 lambda state: (state.has("Keycard A (Facility Item)", player) or state.has("Keycard B (Facility Item)", player)))
    if "Facility - Minimize Facility Scientist Casualties (Agent)" in world.location_name_to_id and is_valid_location(world, "Facility - Minimize Facility Scientist Casualties (Agent)"):
        add_rule(world.multiworld.get_location("Facility - Minimize Facility Scientist Casualties (Agent)", player),
                 lambda state: (state.has("Keycard A (Facility Item)", player) or state.has("Keycard B (Facility Item)", player)))
    if "Facility - Minimize Facility Scientist Casualties (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Facility - Minimize Facility Scientist Casualties (Double Agent)"):
        add_rule(world.multiworld.get_location("Facility - Minimize Facility Scientist Casualties (Double Agent)", player),
                 lambda state: (
                     state.has("Door Decoder (Facility Item)", player) and
                     (state.has("Keycard A (Facility Item)", player) or state.has("Keycard B (Facility Item)", player))
                 ))
    if "Facility - Minimize Facility Scientist Casualties (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Facility - Minimize Facility Scientist Casualties (Secret Agent)"):
        add_rule(world.multiworld.get_location("Facility - Minimize Facility Scientist Casualties (Secret Agent)", player),
                 lambda state: (
                     (state.has("Keycard A (Facility Item)", player) or state.has("Keycard B (Facility Item)", player)) and
                     has_explosive(state, player)
                 ))
    if "Facility - Rendezvous With 006" in world.location_name_to_id and is_valid_location(world, "Facility - Rendezvous With 006"):
        add_rule(world.multiworld.get_location("Facility - Rendezvous With 006", player),
                 lambda state: (state.has("Keycard A (Facility Item)", player) or state.has("Keycard B (Facility Item)", player)))
    if "Facility - Rendezvous With 006 (Agent)" in world.location_name_to_id and is_valid_location(world, "Facility - Rendezvous With 006 (Agent)"):
        add_rule(world.multiworld.get_location("Facility - Rendezvous With 006 (Agent)", player),
                 lambda state: (state.has("Keycard A (Facility Item)", player) or state.has("Keycard B (Facility Item)", player)))
    if "Facility - Rendezvous With 006 (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Facility - Rendezvous With 006 (Double Agent)"):
        add_rule(world.multiworld.get_location("Facility - Rendezvous With 006 (Double Agent)", player),
                 lambda state: (state.has("Keycard A (Facility Item)", player) or state.has("Keycard B (Facility Item)", player)))
    if "Facility - Rendezvous With 006 (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Facility - Rendezvous With 006 (Secret Agent)"):
        add_rule(world.multiworld.get_location("Facility - Rendezvous With 006 (Secret Agent)", player),
                 lambda state: (state.has("Keycard A (Facility Item)", player) or state.has("Keycard B (Facility Item)", player)))
    if "Facility - Secret Agent (Clear)" in world.location_name_to_id and is_valid_location(world, "Facility - Secret Agent (Clear)"):
        add_rule(world.multiworld.get_location("Facility - Secret Agent (Clear)", player),
                 lambda state: (
                     state.has("Door Decoder (Facility Item)", player) and
                     (state.has("Keycard A (Facility Item)", player) or state.has("Keycard B (Facility Item)", player)) and
                     has_explosive(state, player)
                 ))
    if "Frigate - (Clear)" in world.location_name_to_id and is_valid_location(world, "Frigate - (Clear)"):
        add_rule(world.multiworld.get_location("Frigate - (Clear)", player),
                 lambda state: (
                     state.has("Tracker Bug (Frigate Loadout)", player) and
                     has_gun(state, player)
                 ))
    if "Frigate - Agent (Clear)" in world.location_name_to_id and is_valid_location(world, "Frigate - Agent (Clear)"):
        add_rule(world.multiworld.get_location("Frigate - Agent (Clear)", player),
                 lambda state: (
                     state.has("Tracker Bug (Frigate Loadout)", player) and
                     has_gun(state, player)
                 ))
    if "Frigate - Disarm Bridge Bomb (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Frigate - Disarm Bridge Bomb (Double Agent)"):
        add_rule(world.multiworld.get_location("Frigate - Disarm Bridge Bomb (Double Agent)", player),
                 lambda state: state.has("Bomb Defuser (Frigate Loadout)", player))
    if "Frigate - Disarm Bridge Bomb (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Frigate - Disarm Bridge Bomb (Secret Agent)"):
        add_rule(world.multiworld.get_location("Frigate - Disarm Bridge Bomb (Secret Agent)", player),
                 lambda state: state.has("Bomb Defuser (Frigate Loadout)", player))
    if "Frigate - Disarm Engine Room Bomb (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Frigate - Disarm Engine Room Bomb (Double Agent)"):
        add_rule(world.multiworld.get_location("Frigate - Disarm Engine Room Bomb (Double Agent)", player),
                 lambda state: state.has("Bomb Defuser (Frigate Loadout)", player))
    if "Frigate - Disarm Engine Room Bomb (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Frigate - Disarm Engine Room Bomb (Secret Agent)"):
        add_rule(world.multiworld.get_location("Frigate - Disarm Engine Room Bomb (Secret Agent)", player),
                 lambda state: state.has("Bomb Defuser (Frigate Loadout)", player))
    if "Frigate - Double Agent (Clear)" in world.location_name_to_id and is_valid_location(world, "Frigate - Double Agent (Clear)"):
        add_rule(world.multiworld.get_location("Frigate - Double Agent (Clear)", player),
                 lambda state: (
                     state.has("Tracker Bug (Frigate Loadout)", player) and
                     state.has("Bomb Defuser (Frigate Loadout)", player) and
                     has_gun(state, player)
                 ))
    if "Frigate - Plant Tracking Bug On Helicopter (Agent)" in world.location_name_to_id and is_valid_location(world, "Frigate - Plant Tracking Bug On Helicopter (Agent)"):
        add_rule(world.multiworld.get_location("Frigate - Plant Tracking Bug On Helicopter (Agent)", player),
                 lambda state: state.has("Tracker Bug (Frigate Loadout)", player))
    if "Frigate - Plant Tracking Bug On Helicopter (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Frigate - Plant Tracking Bug On Helicopter (Double Agent)"):
        add_rule(world.multiworld.get_location("Frigate - Plant Tracking Bug On Helicopter (Double Agent)", player),
                 lambda state: state.has("Tracker Bug (Frigate Loadout)", player))
    if "Frigate - Plant Tracking Bug On Helicopter (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Frigate - Plant Tracking Bug On Helicopter (Secret Agent)"):
        add_rule(world.multiworld.get_location("Frigate - Plant Tracking Bug On Helicopter (Secret Agent)", player),
                 lambda state: state.has("Tracker Bug (Frigate Loadout)", player))
    if "Frigate - Rescue Hostages" in world.location_name_to_id and is_valid_location(world, "Frigate - Rescue Hostages"):
        add_rule(world.multiworld.get_location("Frigate - Rescue Hostages", player),
                 lambda state: has_gun(state, player))
    if "Frigate - Rescue Hostages (Agent)" in world.location_name_to_id and is_valid_location(world, "Frigate - Rescue Hostages (Agent)"):
        add_rule(world.multiworld.get_location("Frigate - Rescue Hostages (Agent)", player),
                 lambda state: has_gun(state, player))
    if "Frigate - Rescue Hostages (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Frigate - Rescue Hostages (Double Agent)"):
        add_rule(world.multiworld.get_location("Frigate - Rescue Hostages (Double Agent)", player),
                 lambda state: has_gun(state, player))
    if "Frigate - Rescue Hostages (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Frigate - Rescue Hostages (Secret Agent)"):
        add_rule(world.multiworld.get_location("Frigate - Rescue Hostages (Secret Agent)", player),
                 lambda state: has_gun(state, player))
    if "Frigate - Secret Agent (Clear)" in world.location_name_to_id and is_valid_location(world, "Frigate - Secret Agent (Clear)"):
        add_rule(world.multiworld.get_location("Frigate - Secret Agent (Clear)", player),
                 lambda state: (
                     state.has("Tracker Bug (Frigate Loadout)", player) and
                     state.has("Bomb Defuser (Frigate Loadout)", player) and
                     has_gun(state, player)
                 ))
    if "Jungle - (Clear)" in world.location_name_to_id and is_valid_location(world, "Jungle - (Clear)"):
        add_rule(world.multiworld.get_location("Jungle - (Clear)", player),
                 lambda state: has_gun(state, player))
    if "Jungle - Agent (Clear)" in world.location_name_to_id and is_valid_location(world, "Jungle - Agent (Clear)"):
        add_rule(world.multiworld.get_location("Jungle - Agent (Clear)", player),
                 lambda state: has_gun(state, player))
    if "Jungle - Blow Up Ammo Dump" in world.location_name_to_id and is_valid_location(world, "Jungle - Blow Up Ammo Dump"):
        add_rule(world.multiworld.get_location("Jungle - Blow Up Ammo Dump", player),
                 lambda state: has_gun(state, player))
    if "Jungle - Blow Up Ammo Dump (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Jungle - Blow Up Ammo Dump (Double Agent)"):
        add_rule(world.multiworld.get_location("Jungle - Blow Up Ammo Dump (Double Agent)", player),
                 lambda state: has_gun(state, player))
    if "Jungle - Blow Up Ammo Dump (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Jungle - Blow Up Ammo Dump (Secret Agent)"):
        add_rule(world.multiworld.get_location("Jungle - Blow Up Ammo Dump (Secret Agent)", player),
                 lambda state: has_gun(state, player))
    if "Jungle - Destroy Drone Guns" in world.location_name_to_id and is_valid_location(world, "Jungle - Destroy Drone Guns"):
        add_rule(world.multiworld.get_location("Jungle - Destroy Drone Guns", player),
                 lambda state: has_gun(state, player))
    if "Jungle - Destroy Drone Guns (Agent)" in world.location_name_to_id and is_valid_location(world, "Jungle - Destroy Drone Guns (Agent)"):
        add_rule(world.multiworld.get_location("Jungle - Destroy Drone Guns (Agent)", player),
                 lambda state: has_gun(state, player))
    if "Jungle - Destroy Drone Guns (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Jungle - Destroy Drone Guns (Double Agent)"):
        add_rule(world.multiworld.get_location("Jungle - Destroy Drone Guns (Double Agent)", player),
                 lambda state: has_gun(state, player))
    if "Jungle - Destroy Drone Guns (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Jungle - Destroy Drone Guns (Secret Agent)"):
        add_rule(world.multiworld.get_location("Jungle - Destroy Drone Guns (Secret Agent)", player),
                 lambda state: has_gun(state, player))
    if "Jungle - Double Agent (Clear)" in world.location_name_to_id and is_valid_location(world, "Jungle - Double Agent (Clear)"):
        add_rule(world.multiworld.get_location("Jungle - Double Agent (Clear)", player),
                 lambda state: has_gun(state, player))
    if "Jungle - Eliminate Xenia" in world.location_name_to_id and is_valid_location(world, "Jungle - Eliminate Xenia"):
        add_rule(world.multiworld.get_location("Jungle - Eliminate Xenia", player),
                 lambda state: has_gun(state, player))
    if "Jungle - Eliminate Xenia (Agent)" in world.location_name_to_id and is_valid_location(world, "Jungle - Eliminate Xenia (Agent)"):
        add_rule(world.multiworld.get_location("Jungle - Eliminate Xenia (Agent)", player),
                 lambda state: has_gun(state, player))
    if "Jungle - Eliminate Xenia (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Jungle - Eliminate Xenia (Double Agent)"):
        add_rule(world.multiworld.get_location("Jungle - Eliminate Xenia (Double Agent)", player),
                 lambda state: has_gun(state, player))
    if "Jungle - Eliminate Xenia (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Jungle - Eliminate Xenia (Secret Agent)"):
        add_rule(world.multiworld.get_location("Jungle - Eliminate Xenia (Secret Agent)", player),
                 lambda state: has_gun(state, player))
    if "Jungle - Escort Natalya To Janus Base" in world.location_name_to_id and is_valid_location(world, "Jungle - Escort Natalya To Janus Base"):
        add_rule(world.multiworld.get_location("Jungle - Escort Natalya To Janus Base", player),
                 lambda state: has_gun(state, player))
    if "Jungle - Escort Natalya To Janus Base (Agent)" in world.location_name_to_id and is_valid_location(world, "Jungle - Escort Natalya To Janus Base (Agent)"):
        add_rule(world.multiworld.get_location("Jungle - Escort Natalya To Janus Base (Agent)", player),
                 lambda state: has_gun(state, player))
    if "Jungle - Escort Natalya To Janus Base (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Jungle - Escort Natalya To Janus Base (Double Agent)"):
        add_rule(world.multiworld.get_location("Jungle - Escort Natalya To Janus Base (Double Agent)", player),
                 lambda state: has_gun(state, player))
    if "Jungle - Escort Natalya To Janus Base (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Jungle - Escort Natalya To Janus Base (Secret Agent)"):
        add_rule(world.multiworld.get_location("Jungle - Escort Natalya To Janus Base (Secret Agent)", player),
                 lambda state: has_gun(state, player))
    if "Jungle - Secret Agent (Clear)" in world.location_name_to_id and is_valid_location(world, "Jungle - Secret Agent (Clear)"):
        add_rule(world.multiworld.get_location("Jungle - Secret Agent (Clear)", player),
                 lambda state: has_gun(state, player))
    if "Runway - (Clear)" in world.location_name_to_id and is_valid_location(world, "Runway - (Clear)"):
        add_rule(world.multiworld.get_location("Runway - (Clear)", player),
                 lambda state: state.has("Ignition Key (Runway Item)", player))
    if "Runway - Agent (Clear)" in world.location_name_to_id and is_valid_location(world, "Runway - Agent (Clear)"):
        add_rule(world.multiworld.get_location("Runway - Agent (Clear)", player),
                 lambda state: state.has("Ignition Key (Runway Item)", player))
    if "Runway - Destroy Heavy Gun Emplacements" in world.location_name_to_id and is_valid_location(world, "Runway - Destroy Heavy Gun Emplacements"):
        add_rule(world.multiworld.get_location("Runway - Destroy Heavy Gun Emplacements", player),
                 lambda state: has_explosive(state, player))
    if "Runway - Destroy Heavy Gun Emplacements (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Runway - Destroy Heavy Gun Emplacements (Double Agent)"):
        add_rule(world.multiworld.get_location("Runway - Destroy Heavy Gun Emplacements (Double Agent)", player),
                 lambda state: has_explosive(state, player))
    if "Runway - Destroy Missile Battery" in world.location_name_to_id and is_valid_location(world, "Runway - Destroy Missile Battery"):
        add_rule(world.multiworld.get_location("Runway - Destroy Missile Battery", player),
                 lambda state: has_explosive(state, player))
    if "Runway - Destroy Missile Battery (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Runway - Destroy Missile Battery (Double Agent)"):
        add_rule(world.multiworld.get_location("Runway - Destroy Missile Battery (Double Agent)", player),
                 lambda state: has_explosive(state, player))
    if "Runway - Destroy Missile Battery (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Runway - Destroy Missile Battery (Secret Agent)"):
        add_rule(world.multiworld.get_location("Runway - Destroy Missile Battery (Secret Agent)", player),
                 lambda state: has_explosive(state, player))
    if "Runway - Double Agent (Clear)" in world.location_name_to_id and is_valid_location(world, "Runway - Double Agent (Clear)"):
        add_rule(world.multiworld.get_location("Runway - Double Agent (Clear)", player),
                 lambda state: (
                     state.has("Ignition Key (Runway Item)", player) and
                     has_explosive(state, player)
                 ))
    if "Runway - Escape In Plane" in world.location_name_to_id and is_valid_location(world, "Runway - Escape In Plane"):
        add_rule(world.multiworld.get_location("Runway - Escape In Plane", player),
                 lambda state: state.has("Ignition Key (Runway Item)", player))
    if "Runway - Escape In Plane (Agent)" in world.location_name_to_id and is_valid_location(world, "Runway - Escape In Plane (Agent)"):
        add_rule(world.multiworld.get_location("Runway - Escape In Plane (Agent)", player),
                 lambda state: state.has("Ignition Key (Runway Item)", player))
    if "Runway - Escape In Plane (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Runway - Escape In Plane (Double Agent)"):
        add_rule(world.multiworld.get_location("Runway - Escape In Plane (Double Agent)", player),
                 lambda state: state.has("Ignition Key (Runway Item)", player))
    if "Runway - Escape In Plane (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Runway - Escape In Plane (Secret Agent)"):
        add_rule(world.multiworld.get_location("Runway - Escape In Plane (Secret Agent)", player),
                 lambda state: state.has("Ignition Key (Runway Item)", player))
    if "Runway - Find Plane Ignition Key" in world.location_name_to_id and is_valid_location(world, "Runway - Find Plane Ignition Key"):
        add_rule(world.multiworld.get_location("Runway - Find Plane Ignition Key", player),
                 lambda state: state.has("Ignition Key (Runway Item)", player))
    if "Runway - Find Plane Ignition Key (Agent)" in world.location_name_to_id and is_valid_location(world, "Runway - Find Plane Ignition Key (Agent)"):
        add_rule(world.multiworld.get_location("Runway - Find Plane Ignition Key (Agent)", player),
                 lambda state: state.has("Ignition Key (Runway Item)", player))
    if "Runway - Find Plane Ignition Key (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Runway - Find Plane Ignition Key (Double Agent)"):
        add_rule(world.multiworld.get_location("Runway - Find Plane Ignition Key (Double Agent)", player),
                 lambda state: state.has("Ignition Key (Runway Item)", player))
    if "Runway - Find Plane Ignition Key (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Runway - Find Plane Ignition Key (Secret Agent)"):
        add_rule(world.multiworld.get_location("Runway - Find Plane Ignition Key (Secret Agent)", player),
                 lambda state: state.has("Ignition Key (Runway Item)", player))
    if "Runway - Secret Agent (Clear)" in world.location_name_to_id and is_valid_location(world, "Runway - Secret Agent (Clear)"):
        add_rule(world.multiworld.get_location("Runway - Secret Agent (Clear)", player),
                 lambda state: (
                     state.has("Ignition Key (Runway Item)", player) and
                     has_explosive(state, player)
                 ))
    if "Silo - (Clear)" in world.location_name_to_id and is_valid_location(world, "Silo - (Clear)"):
        add_rule(world.multiworld.get_location("Silo - (Clear)", player),
                 lambda state: (
                     state.has("Keycard Level 1 (Silo Item)", player) and
                     state.has("Keycard Level 2 (Silo Item)", player) and
                     state.has("Keycard Level 3 (Silo Item)", player) and
                     has_gun(state, player)
                 ))
    if "Silo - Agent (Clear)" in world.location_name_to_id and is_valid_location(world, "Silo - Agent (Clear)"):
        add_rule(world.multiworld.get_location("Silo - Agent (Clear)", player),
                 lambda state: (
                     state.has("Keycard Level 1 (Silo Item)", player) and
                     state.has("Keycard Level 2 (Silo Item)", player) and
                     state.has("Keycard Level 3 (Silo Item)", player) and
                     has_gun(state, player)
                 ))
    if "Silo - CPU Circuit Board (Agent)" in world.location_name_to_id and is_valid_location(world, "Silo - CPU Circuit Board (Agent)"):
        add_rule(world.multiworld.get_location("Silo - CPU Circuit Board (Agent)", player),
                 lambda state: has_gun(state, player))
    if "Silo - CPU Circuit Board (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Silo - CPU Circuit Board (Double Agent)"):
        add_rule(world.multiworld.get_location("Silo - CPU Circuit Board (Double Agent)", player),
                 lambda state: has_gun(state, player))
    if "Silo - CPU Circuit Board (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Silo - CPU Circuit Board (Secret Agent)"):
        add_rule(world.multiworld.get_location("Silo - CPU Circuit Board (Secret Agent)", player),
                 lambda state: has_gun(state, player))
    if "Silo - DAT (Agent)" in world.location_name_to_id and is_valid_location(world, "Silo - DAT (Agent)"):
        add_rule(world.multiworld.get_location("Silo - DAT (Agent)", player),
                 lambda state: (
                     state.has("Keycard Level 1 (Silo Item)", player) and
                     state.has("Keycard Level 2 (Silo Item)", player) and
                     has_gun(state, player)
                 ))
    if "Silo - DAT (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Silo - DAT (Double Agent)"):
        add_rule(world.multiworld.get_location("Silo - DAT (Double Agent)", player),
                 lambda state: (
                     state.has("Keycard Level 1 (Silo Item)", player) and
                     state.has("Keycard Level 2 (Silo Item)", player) and
                     has_gun(state, player)
                 ))
    if "Silo - DAT (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Silo - DAT (Secret Agent)"):
        add_rule(world.multiworld.get_location("Silo - DAT (Secret Agent)", player),
                 lambda state: (
                     state.has("Keycard Level 1 (Silo Item)", player) and
                     state.has("Keycard Level 2 (Silo Item)", player) and
                     has_gun(state, player)
                 ))
    if "Silo - Double Agent (Clear)" in world.location_name_to_id and is_valid_location(world, "Silo - Double Agent (Clear)"):
        add_rule(world.multiworld.get_location("Silo - Double Agent (Clear)", player),
                 lambda state: (
                     state.has("Camera (Bunker 1 / Silo Loadout)", player) and
                     state.has("I/O Circuit Board (Silo Item)", player) and
                     state.has("CPU Circuit Board (Silo Item)", player) and
                     state.has("RSP Circuit Board (Silo Item)", player) and
                     state.has("RDP Circuit Board (Silo Item)", player) and
                     state.has("DAT (Silo Item)", player) and
                     state.has("Keycard Level 1 (Silo Item)", player) and
                     state.has("Keycard Level 2 (Silo Item)", player) and
                     state.has("Keycard Level 3 (Silo Item)", player) and
                     state.has("Plastique (Silo Loadout)", player) and
                     has_gun(state, player)
                 ))
    if "Silo - I/O Circuit Board (Agent)" in world.location_name_to_id and is_valid_location(world, "Silo - I/O Circuit Board (Agent)"):
        add_rule(world.multiworld.get_location("Silo - I/O Circuit Board (Agent)", player),
                 lambda state: has_gun(state, player))
    if "Silo - I/O Circuit Board (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Silo - I/O Circuit Board (Double Agent)"):
        add_rule(world.multiworld.get_location("Silo - I/O Circuit Board (Double Agent)", player),
                 lambda state: has_gun(state, player))
    if "Silo - I/O Circuit Board (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Silo - I/O Circuit Board (Secret Agent)"):
        add_rule(world.multiworld.get_location("Silo - I/O Circuit Board (Secret Agent)", player),
                 lambda state: has_gun(state, player))
    if "Silo - Keycard Silo Level 1 (Agent)" in world.location_name_to_id and is_valid_location(world, "Silo - Keycard Silo Level 1 (Agent)"):
        add_rule(world.multiworld.get_location("Silo - Keycard Silo Level 1 (Agent)", player),
                 lambda state: has_gun(state, player))
    if "Silo - Keycard Silo Level 1 (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Silo - Keycard Silo Level 1 (Double Agent)"):
        add_rule(world.multiworld.get_location("Silo - Keycard Silo Level 1 (Double Agent)", player),
                 lambda state: has_gun(state, player))
    if "Silo - Keycard Silo Level 1 (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Silo - Keycard Silo Level 1 (Secret Agent)"):
        add_rule(world.multiworld.get_location("Silo - Keycard Silo Level 1 (Secret Agent)", player),
                 lambda state: has_gun(state, player))
    if "Silo - Keycard Silo Level 2 (Agent)" in world.location_name_to_id and is_valid_location(world, "Silo - Keycard Silo Level 2 (Agent)"):
        add_rule(world.multiworld.get_location("Silo - Keycard Silo Level 2 (Agent)", player),
                 lambda state: (
                     state.has("Keycard Level 1 (Silo Item)", player) and
                     has_gun(state, player)
                 ))
    if "Silo - Keycard Silo Level 2 (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Silo - Keycard Silo Level 2 (Double Agent)"):
        add_rule(world.multiworld.get_location("Silo - Keycard Silo Level 2 (Double Agent)", player),
                 lambda state: (
                     state.has("Keycard Level 1 (Silo Item)", player) and
                     has_gun(state, player)
                 ))
    if "Silo - Keycard Silo Level 2 (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Silo - Keycard Silo Level 2 (Secret Agent)"):
        add_rule(world.multiworld.get_location("Silo - Keycard Silo Level 2 (Secret Agent)", player),
                 lambda state: (
                     state.has("Keycard Level 1 (Silo Item)", player) and
                     has_gun(state, player)
                 ))
    if "Silo - Keycard Silo Level 3 (Agent)" in world.location_name_to_id and is_valid_location(world, "Silo - Keycard Silo Level 3 (Agent)"):
        add_rule(world.multiworld.get_location("Silo - Keycard Silo Level 3 (Agent)", player),
                 lambda state: (
                     state.has("Keycard Level 1 (Silo Item)", player) and
                     state.has("Keycard Level 2 (Silo Item)", player) and
                     has_gun(state, player)
                 ))
    if "Silo - Keycard Silo Level 3 (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Silo - Keycard Silo Level 3 (Double Agent)"):
        add_rule(world.multiworld.get_location("Silo - Keycard Silo Level 3 (Double Agent)", player),
                 lambda state: (
                     state.has("Keycard Level 1 (Silo Item)", player) and
                     state.has("Keycard Level 2 (Silo Item)", player) and
                     has_gun(state, player)
                 ))
    if "Silo - Keycard Silo Level 3 (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Silo - Keycard Silo Level 3 (Secret Agent)"):
        add_rule(world.multiworld.get_location("Silo - Keycard Silo Level 3 (Secret Agent)", player),
                 lambda state: (
                     state.has("Keycard Level 1 (Silo Item)", player) and
                     state.has("Keycard Level 2 (Silo Item)", player) and
                     has_gun(state, player)
                 ))
    if "Silo - Minimize Silo Scientist Casualties" in world.location_name_to_id and is_valid_location(world, "Silo - Minimize Silo Scientist Casualties"):
        add_rule(world.multiworld.get_location("Silo - Minimize Silo Scientist Casualties", player),
                 lambda state: has_gun(state, player))
    if "Silo - Minimize Silo Scientist Casualties (Agent)" in world.location_name_to_id and is_valid_location(world, "Silo - Minimize Silo Scientist Casualties (Agent)"):
        add_rule(world.multiworld.get_location("Silo - Minimize Silo Scientist Casualties (Agent)", player),
                 lambda state: (
                     state.has("Keycard Level 1 (Silo Item)", player) and
                     state.has("Keycard Level 2 (Silo Item)", player) and
                     state.has("Keycard Level 3 (Silo Item)", player) and
                     has_gun(state, player)
                 ))
    if "Silo - Minimize Silo Scientist Casualties (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Silo - Minimize Silo Scientist Casualties (Double Agent)"):
        add_rule(world.multiworld.get_location("Silo - Minimize Silo Scientist Casualties (Double Agent)", player),
                 lambda state: (
                     state.has("Keycard Level 1 (Silo Item)", player) and
                     state.has("Keycard Level 2 (Silo Item)", player) and
                     state.has("Keycard Level 3 (Silo Item)", player) and
                     has_gun(state, player)
                 ))
    if "Silo - Minimize Silo Scientist Casualties (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Silo - Minimize Silo Scientist Casualties (Secret Agent)"):
        add_rule(world.multiworld.get_location("Silo - Minimize Silo Scientist Casualties (Secret Agent)", player),
                 lambda state: (
                     state.has("Keycard Level 1 (Silo Item)", player) and
                     state.has("Keycard Level 2 (Silo Item)", player) and
                     state.has("Keycard Level 3 (Silo Item)", player) and
                     has_gun(state, player)
                 ))
    if "Silo - Obtain Telemetric Data" in world.location_name_to_id and is_valid_location(world, "Silo - Obtain Telemetric Data"):
        add_rule(world.multiworld.get_location("Silo - Obtain Telemetric Data", player),
                 lambda state: has_gun(state, player))
    if "Silo - Obtain Telemetric Data (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Silo - Obtain Telemetric Data (Double Agent)"):
        add_rule(world.multiworld.get_location("Silo - Obtain Telemetric Data (Double Agent)", player),
                 lambda state: (
                     state.has("Keycard Level 1 (Silo Item)", player) and
                     state.has("Keycard Level 2 (Silo Item)", player) and
                     state.has("DAT (Silo Item)", player) and
                     has_gun(state, player)
                 ))
    if "Silo - Obtain Telemetric Data (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Silo - Obtain Telemetric Data (Secret Agent)"):
        add_rule(world.multiworld.get_location("Silo - Obtain Telemetric Data (Secret Agent)", player),
                 lambda state: (
                     state.has("Keycard Level 1 (Silo Item)", player) and
                     state.has("Keycard Level 2 (Silo Item)", player) and
                     state.has("DAT (Silo Item)", player) and
                     has_gun(state, player)
                 ))
    if "Silo - Photograph Satellite" in world.location_name_to_id and is_valid_location(world, "Silo - Photograph Satellite"):
        add_rule(world.multiworld.get_location("Silo - Photograph Satellite", player),
                 lambda state: has_gun(state, player))
    if "Silo - Photograph Satellite (Agent)" in world.location_name_to_id and is_valid_location(world, "Silo - Photograph Satellite (Agent)"):
        add_rule(world.multiworld.get_location("Silo - Photograph Satellite (Agent)", player),
                 lambda state: (
                     state.has("Camera (Bunker 1 / Silo Loadout)", player) and
                     state.has("Keycard Level 1 (Silo Item)", player) and
                     state.has("Keycard Level 2 (Silo Item)", player) and
                     state.has("Keycard Level 3 (Silo Item)", player) and
                     has_gun(state, player)
                 ))
    if "Silo - Photograph Satellite (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Silo - Photograph Satellite (Double Agent)"):
        add_rule(world.multiworld.get_location("Silo - Photograph Satellite (Double Agent)", player),
                 lambda state: (
                     state.has("Camera (Bunker 1 / Silo Loadout)", player) and
                     state.has("Keycard Level 1 (Silo Item)", player) and
                     state.has("Keycard Level 2 (Silo Item)", player) and
                     state.has("Keycard Level 3 (Silo Item)", player) and
                     has_gun(state, player)
                 ))
    if "Silo - Photograph Satellite (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Silo - Photograph Satellite (Secret Agent)"):
        add_rule(world.multiworld.get_location("Silo - Photograph Satellite (Secret Agent)", player),
                 lambda state: (
                     state.has("Camera (Bunker 1 / Silo Loadout)", player) and
                     state.has("Keycard Level 1 (Silo Item)", player) and
                     state.has("Keycard Level 2 (Silo Item)", player) and
                     state.has("Keycard Level 3 (Silo Item)", player) and
                     has_gun(state, player)
                 ))
    if "Silo - Plant Bombs In Fuel Rooms" in world.location_name_to_id and is_valid_location(world, "Silo - Plant Bombs In Fuel Rooms"):
        add_rule(world.multiworld.get_location("Silo - Plant Bombs In Fuel Rooms", player),
                 lambda state: has_gun(state, player))
    if "Silo - Plant Bombs In Fuel Rooms (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Silo - Plant Bombs In Fuel Rooms (Double Agent)"):
        add_rule(world.multiworld.get_location("Silo - Plant Bombs In Fuel Rooms (Double Agent)", player),
                 lambda state: (
                     state.has("Keycard Level 1 (Silo Item)", player) and
                     state.has("Keycard Level 2 (Silo Item)", player) and
                     state.has("Keycard Level 3 (Silo Item)", player) and
                     state.has("Plastique (Silo Loadout)", player) and
                     has_gun(state, player)
                 ))
    if "Silo - RDP Circuit Board (Agent)" in world.location_name_to_id and is_valid_location(world, "Silo - RDP Circuit Board (Agent)"):
        add_rule(world.multiworld.get_location("Silo - RDP Circuit Board (Agent)", player),
                 lambda state: (
                     state.has("Keycard Level 1 (Silo Item)", player) and
                     state.has("Keycard Level 2 (Silo Item)", player) and
                     has_gun(state, player)
                 ))
    if "Silo - RDP Circuit Board (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Silo - RDP Circuit Board (Double Agent)"):
        add_rule(world.multiworld.get_location("Silo - RDP Circuit Board (Double Agent)", player),
                 lambda state: (
                     state.has("Keycard Level 1 (Silo Item)", player) and
                     state.has("Keycard Level 2 (Silo Item)", player) and
                     has_gun(state, player)
                 ))
    if "Silo - RDP Circuit Board (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Silo - RDP Circuit Board (Secret Agent)"):
        add_rule(world.multiworld.get_location("Silo - RDP Circuit Board (Secret Agent)", player),
                 lambda state: (
                     state.has("Keycard Level 1 (Silo Item)", player) and
                     state.has("Keycard Level 2 (Silo Item)", player) and
                     has_gun(state, player)
                 ))
    if "Silo - RSP Circuit Board (Agent)" in world.location_name_to_id and is_valid_location(world, "Silo - RSP Circuit Board (Agent)"):
        add_rule(world.multiworld.get_location("Silo - RSP Circuit Board (Agent)", player),
                 lambda state: (
                     state.has("Keycard Level 1 (Silo Item)", player) and
                     has_gun(state, player)
                 ))
    if "Silo - RSP Circuit Board (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Silo - RSP Circuit Board (Double Agent)"):
        add_rule(world.multiworld.get_location("Silo - RSP Circuit Board (Double Agent)", player),
                 lambda state: (
                     state.has("Keycard Level 1 (Silo Item)", player) and
                     has_gun(state, player)
                 ))
    if "Silo - RSP Circuit Board (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Silo - RSP Circuit Board (Secret Agent)"):
        add_rule(world.multiworld.get_location("Silo - RSP Circuit Board (Secret Agent)", player),
                 lambda state: (
                     state.has("Keycard Level 1 (Silo Item)", player) and
                     has_gun(state, player)
                 ))
    if "Silo - Retrieve Satellite Circuitry" in world.location_name_to_id and is_valid_location(world, "Silo - Retrieve Satellite Circuitry"):
        add_rule(world.multiworld.get_location("Silo - Retrieve Satellite Circuitry", player),
                 lambda state: has_gun(state, player))
    if "Silo - Retrieve Satellite Circuitry (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Silo - Retrieve Satellite Circuitry (Double Agent)"):
        add_rule(world.multiworld.get_location("Silo - Retrieve Satellite Circuitry (Double Agent)", player),
                 lambda state: (
                     state.has("I/O Circuit Board (Silo Item)", player) and
                     state.has("CPU Circuit Board (Silo Item)", player) and
                     state.has("RSP Circuit Board (Silo Item)", player) and
                     state.has("RDP Circuit Board (Silo Item)", player) and
                     state.has("Keycard Level 1 (Silo Item)", player) and
                     state.has("Keycard Level 2 (Silo Item)", player) and
                     has_gun(state, player)
                 ))
    if "Silo - Retrieve Satellite Circuitry (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Silo - Retrieve Satellite Circuitry (Secret Agent)"):
        add_rule(world.multiworld.get_location("Silo - Retrieve Satellite Circuitry (Secret Agent)", player),
                 lambda state: (
                     state.has("I/O Circuit Board (Silo Item)", player) and
                     state.has("CPU Circuit Board (Silo Item)", player) and
                     state.has("RSP Circuit Board (Silo Item)", player) and
                     state.has("RDP Circuit Board (Silo Item)", player) and
                     state.has("Keycard Level 1 (Silo Item)", player) and
                     state.has("Keycard Level 2 (Silo Item)", player) and
                     has_gun(state, player)
                 ))
    if "Silo - Secret Agent (Clear)" in world.location_name_to_id and is_valid_location(world, "Silo - Secret Agent (Clear)"):
        add_rule(world.multiworld.get_location("Silo - Secret Agent (Clear)", player),
                 lambda state: (
                     state.has("I/O Circuit Board (Silo Item)", player) and
                     state.has("CPU Circuit Board (Silo Item)", player) and
                     state.has("RSP Circuit Board (Silo Item)", player) and
                     state.has("RDP Circuit Board (Silo Item)", player) and
                     state.has("DAT (Silo Item)", player) and
                     state.has("Keycard Level 1 (Silo Item)", player) and
                     state.has("Keycard Level 2 (Silo Item)", player) and
                     state.has("Keycard Level 3 (Silo Item)", player) and
                     has_gun(state, player)
                 ))
    if "Statue - (Clear)" in world.location_name_to_id and is_valid_location(world, "Statue - (Clear)"):
        add_rule(world.multiworld.get_location("Statue - (Clear)", player),
                 lambda state: state.has("Flight Recorder (Archives Item)", player))
    if "Statue - Agent (Clear)" in world.location_name_to_id and is_valid_location(world, "Statue - Agent (Clear)"):
        add_rule(world.multiworld.get_location("Statue - Agent (Clear)", player),
                 lambda state: state.has("Flight Recorder (Archives Item)", player))
    if "Statue - Double Agent (Clear)" in world.location_name_to_id and is_valid_location(world, "Statue - Double Agent (Clear)"):
        add_rule(world.multiworld.get_location("Statue - Double Agent (Clear)", player),
                 lambda state: state.has("Flight Recorder (Archives Item)", player))
    if "Statue - Find Flight Recorder (Agent)" in world.location_name_to_id and is_valid_location(world, "Statue - Find Flight Recorder (Agent)"):
        add_rule(world.multiworld.get_location("Statue - Find Flight Recorder (Agent)", player),
                 lambda state: state.has("Flight Recorder (Archives Item)", player))
    if "Statue - Find Flight Recorder (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Statue - Find Flight Recorder (Double Agent)"):
        add_rule(world.multiworld.get_location("Statue - Find Flight Recorder (Double Agent)", player),
                 lambda state: state.has("Flight Recorder (Archives Item)", player))
    if "Statue - Find Flight Recorder (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Statue - Find Flight Recorder (Secret Agent)"):
        add_rule(world.multiworld.get_location("Statue - Find Flight Recorder (Secret Agent)", player),
                 lambda state: state.has("Flight Recorder (Archives Item)", player))
    if "Statue - Secret Agent (Clear)" in world.location_name_to_id and is_valid_location(world, "Statue - Secret Agent (Clear)"):
        add_rule(world.multiworld.get_location("Statue - Secret Agent (Clear)", player),
                 lambda state: state.has("Flight Recorder (Archives Item)", player))
    if "Surface 1 - (Clear)" in world.location_name_to_id and is_valid_location(world, "Surface 1 - (Clear)"):
        add_rule(world.multiworld.get_location("Surface 1 - (Clear)", player),
                 lambda state: state.has("Hut Door Key (Surface 1 Item)", player))
    if "Surface 1 - Agent (Clear)" in world.location_name_to_id and is_valid_location(world, "Surface 1 - Agent (Clear)"):
        add_rule(world.multiworld.get_location("Surface 1 - Agent (Clear)", player),
                 lambda state: state.has("Hut Door Key (Surface 1 Item)", player))
    if "Surface 1 - Bunker Plans (Agent)" in world.location_name_to_id and is_valid_location(world, "Surface 1 - Bunker Plans (Agent)"):
        add_rule(world.multiworld.get_location("Surface 1 - Bunker Plans (Agent)", player),
                 lambda state: (
                     state.has("Hut Door Key (Surface 1 Item)", player) and
                     state.has("Safe Key (Surface 1 Item)", player)
                 ))
    if "Surface 1 - Bunker Plans (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Surface 1 - Bunker Plans (Double Agent)"):
        add_rule(world.multiworld.get_location("Surface 1 - Bunker Plans (Double Agent)", player),
                 lambda state: (
                     state.has("Hut Door Key (Surface 1 Item)", player) and
                     state.has("Safe Key (Surface 1 Item)", player)
                 ))
    if "Surface 1 - Bunker Plans (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Surface 1 - Bunker Plans (Secret Agent)"):
        add_rule(world.multiworld.get_location("Surface 1 - Bunker Plans (Secret Agent)", player),
                 lambda state: (
                     state.has("Hut Door Key (Surface 1 Item)", player) and
                     state.has("Safe Key (Surface 1 Item)", player)
                 ))
    if "Surface 1 - Double Agent (Clear)" in world.location_name_to_id and is_valid_location(world, "Surface 1 - Double Agent (Clear)"):
        add_rule(world.multiworld.get_location("Surface 1 - Double Agent (Clear)", player),
                 lambda state: (
                     state.has("Hut Door Key (Surface 1 Item)", player) and
                     state.has("Safe Key (Surface 1 Item)", player) and
                     state.has("Building Plans (Surface 1 Item)", player)
                 ))
    if "Surface 1 - Obtain Surface Safe Key (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Surface 1 - Obtain Surface Safe Key (Double Agent)"):
        add_rule(world.multiworld.get_location("Surface 1 - Obtain Surface Safe Key (Double Agent)", player),
                 lambda state: state.has("|Safe Key (Surface 1 Item)", player))
    if "Surface 1 - Obtain Surface Safe Key (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Surface 1 - Obtain Surface Safe Key (Secret Agent)"):
        add_rule(world.multiworld.get_location("Surface 1 - Obtain Surface Safe Key (Secret Agent)", player),
                 lambda state: state.has("|Safe Key (Surface 1 Item)", player))
    if "Surface 1 - Power Down Communications Dish" in world.location_name_to_id and is_valid_location(world, "Surface 1 - Power Down Communications Dish"):
        add_rule(world.multiworld.get_location("Surface 1 - Power Down Communications Dish", player),
                 lambda state: state.has("Hut Door Key (Surface 1 Item)", player))
    if "Surface 1 - Power Down Communications Dish (Agent)" in world.location_name_to_id and is_valid_location(world, "Surface 1 - Power Down Communications Dish (Agent)"):
        add_rule(world.multiworld.get_location("Surface 1 - Power Down Communications Dish (Agent)", player),
                 lambda state: state.has("Hut Door Key (Surface 1 Item)", player))
    if "Surface 1 - Power Down Communications Dish (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Surface 1 - Power Down Communications Dish (Double Agent)"):
        add_rule(world.multiworld.get_location("Surface 1 - Power Down Communications Dish (Double Agent)", player),
                 lambda state: state.has("Hut Door Key (Surface 1 Item)", player))
    if "Surface 1 - Power Down Communications Dish (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Surface 1 - Power Down Communications Dish (Secret Agent)"):
        add_rule(world.multiworld.get_location("Surface 1 - Power Down Communications Dish (Secret Agent)", player),
                 lambda state: state.has("Hut Door Key (Surface 1 Item)", player))
    if "Surface 1 - Safe Key (Agent)" in world.location_name_to_id and is_valid_location(world, "Surface 1 - Safe Key (Agent)"):
        add_rule(world.multiworld.get_location("Surface 1 - Safe Key (Agent)", player),
                 lambda state: state.has("Hut Door Key (Surface 1 Item)", player))
    if "Surface 1 - Safe Key (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Surface 1 - Safe Key (Double Agent)"):
        add_rule(world.multiworld.get_location("Surface 1 - Safe Key (Double Agent)", player),
                 lambda state: state.has("Hut Door Key (Surface 1 Item)", player))
    if "Surface 1 - Safe Key (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Surface 1 - Safe Key (Secret Agent)"):
        add_rule(world.multiworld.get_location("Surface 1 - Safe Key (Secret Agent)", player),
                 lambda state: state.has("Hut Door Key (Surface 1 Item)", player))
    if "Surface 1 - Secret Agent (Clear)" in world.location_name_to_id and is_valid_location(world, "Surface 1 - Secret Agent (Clear)"):
        add_rule(world.multiworld.get_location("Surface 1 - Secret Agent (Clear)", player),
                 lambda state: (
                     state.has("Hut Door Key (Surface 1 Item)", player) and
                     state.has("Safe Key (Surface 1 Item)", player) and
                     state.has("Building Plans (Surface 1 Item)", player)
                 ))
    if "Surface 1 - Steal Building Plans" in world.location_name_to_id and is_valid_location(world, "Surface 1 - Steal Building Plans"):
        add_rule(world.multiworld.get_location("Surface 1 - Steal Building Plans", player),
                 lambda state: (
                     state.has("Hut Door Key (Surface 1 Item)", player) and
                     state.has("Safe Key (Surface 1 Item)", player) and
                     state.has("Building Plans (Surface 1 Item)", player)
                 ))
    if "Surface 1 - Steal Building Plans (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Surface 1 - Steal Building Plans (Double Agent)"):
        add_rule(world.multiworld.get_location("Surface 1 - Steal Building Plans (Double Agent)", player),
                 lambda state: state.has("|Safe Key (Surface 1 Item)", player))
    if "Surface 1 - Steal Building Plans (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Surface 1 - Steal Building Plans (Secret Agent)"):
        add_rule(world.multiworld.get_location("Surface 1 - Steal Building Plans (Secret Agent)", player),
                 lambda state: state.has("|Safe Key (Surface 1 Item)", player))
    if "Surface 2 - (Clear)" in world.location_name_to_id and is_valid_location(world, "Surface 2 - (Clear)"):
        add_rule(world.multiworld.get_location("Surface 2 - (Clear)", player),
                 lambda state: state.has("Hut Door Key (Surface 1 Item)", player))
    if "Surface 2 - Agent (Clear)" in world.location_name_to_id and is_valid_location(world, "Surface 2 - Agent (Clear)"):
        add_rule(world.multiworld.get_location("Surface 2 - Agent (Clear)", player),
                 lambda state: state.has("Hut Door Key (Surface 1 Item)", player))
    if "Surface 2 - Break Communications Link To Bunker (Agent)" in world.location_name_to_id and is_valid_location(world, "Surface 2 - Break Communications Link To Bunker (Agent)"):
        add_rule(world.multiworld.get_location("Surface 2 - Break Communications Link To Bunker (Agent)", player),
                 lambda state: state.has("Hut Door Key (Surface 1 Item)", player))
    if "Surface 2 - Break Communications Link To Bunker (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Surface 2 - Break Communications Link To Bunker (Double Agent)"):
        add_rule(world.multiworld.get_location("Surface 2 - Break Communications Link To Bunker (Double Agent)", player),
                 lambda state: state.has("Hut Door Key (Surface 1 Item)", player))
    if "Surface 2 - Break Communications Link To Bunker (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Surface 2 - Break Communications Link To Bunker (Secret Agent)"):
        add_rule(world.multiworld.get_location("Surface 2 - Break Communications Link To Bunker (Secret Agent)", player),
                 lambda state: state.has("Hut Door Key (Surface 1 Item)", player))
    if "Surface 2 - Disable Spetznaz Support Aircraft (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Surface 2 - Disable Spetznaz Support Aircraft (Double Agent)"):
        add_rule(world.multiworld.get_location("Surface 2 - Disable Spetznaz Support Aircraft (Double Agent)", player),
                 lambda state: state.has("Special Timed Mine (Surface 2 Loadout)", player))
    if "Surface 2 - Disable Spetznaz Support Aircraft (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Surface 2 - Disable Spetznaz Support Aircraft (Secret Agent)"):
        add_rule(world.multiworld.get_location("Surface 2 - Disable Spetznaz Support Aircraft (Secret Agent)", player),
                 lambda state: state.has("Special Timed Mine (Surface 2 Loadout)", player))
    if "Surface 2 - Double Agent (Clear)" in world.location_name_to_id and is_valid_location(world, "Surface 2 - Double Agent (Clear)"):
        add_rule(world.multiworld.get_location("Surface 2 - Double Agent (Clear)", player),
                 lambda state: (
                     state.has("Special Timed Mine (Surface 2 Loadout)", player) and
                     state.has("Hut Door Key (Surface 1 Item)", player)
                 ))
    if "Surface 2 - Secret Agent (Clear)" in world.location_name_to_id and is_valid_location(world, "Surface 2 - Secret Agent (Clear)"):
        add_rule(world.multiworld.get_location("Surface 2 - Secret Agent (Clear)", player),
                 lambda state: (
                     state.has("Special Timed Mine (Surface 2 Loadout)", player) and
                     state.has("Hut Door Key (Surface 1 Item)", player)
                 ))
    if "Train - (Clear)" in world.location_name_to_id and is_valid_location(world, "Train - (Clear)"):
        add_rule(world.multiworld.get_location("Train - (Clear)", player),
                 lambda state: (
                     state.has("Train Door Key (Depot Item)", player) and
                     has_gun(state, player)
                 ))
    if "Train - Agent (Clear)" in world.location_name_to_id and is_valid_location(world, "Train - Agent (Clear)"):
        add_rule(world.multiworld.get_location("Train - Agent (Clear)", player),
                 lambda state: (
                     state.has("Train Door Key (Depot Item)", player) and
                     has_gun(state, player)
                 ))
    if "Train - Crack Boris' Password" in world.location_name_to_id and is_valid_location(world, "Train - Crack Boris' Password"):
        add_rule(world.multiworld.get_location("Train - Crack Boris' Password", player),
                 lambda state: has_gun(state, player))
    if "Train - Crack Boris' Password (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Train - Crack Boris' Password (Double Agent)"):
        add_rule(world.multiworld.get_location("Train - Crack Boris' Password (Double Agent)", player),
                 lambda state: (
                     state.has("Train Door Key (Depot Item)", player) and
                     has_gun(state, player)
                 ))
    if "Train - Destroy Brake Units" in world.location_name_to_id and is_valid_location(world, "Train - Destroy Brake Units"):
        add_rule(world.multiworld.get_location("Train - Destroy Brake Units", player),
                 lambda state: has_gun(state, player))
    if "Train - Destroy Brake Units (Agent)" in world.location_name_to_id and is_valid_location(world, "Train - Destroy Brake Units (Agent)"):
        add_rule(world.multiworld.get_location("Train - Destroy Brake Units (Agent)", player),
                 lambda state: has_gun(state, player))
    if "Train - Destroy Brake Units (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Train - Destroy Brake Units (Double Agent)"):
        add_rule(world.multiworld.get_location("Train - Destroy Brake Units (Double Agent)", player),
                 lambda state: has_gun(state, player))
    if "Train - Destroy Brake Units (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Train - Destroy Brake Units (Secret Agent)"):
        add_rule(world.multiworld.get_location("Train - Destroy Brake Units (Secret Agent)", player),
                 lambda state: has_gun(state, player))
    if "Train - Double Agent (Clear)" in world.location_name_to_id and is_valid_location(world, "Train - Double Agent (Clear)"):
        add_rule(world.multiworld.get_location("Train - Double Agent (Clear)", player),
                 lambda state: (
                     state.has("Train Door Key (Depot Item)", player) and
                     has_gun(state, player)
                 ))
    if "Train - Escape To Safety" in world.location_name_to_id and is_valid_location(world, "Train - Escape To Safety"):
        add_rule(world.multiworld.get_location("Train - Escape To Safety", player),
                 lambda state: has_gun(state, player))
    if "Train - Escape To Safety (Agent)" in world.location_name_to_id and is_valid_location(world, "Train - Escape To Safety (Agent)"):
        add_rule(world.multiworld.get_location("Train - Escape To Safety (Agent)", player),
                 lambda state: (
                     state.has("Train Door Key (Depot Item)", player) and
                     has_gun(state, player)
                 ))
    if "Train - Escape To Safety (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Train - Escape To Safety (Double Agent)"):
        add_rule(world.multiworld.get_location("Train - Escape To Safety (Double Agent)", player),
                 lambda state: (
                     state.has("Train Door Key (Depot Item)", player) and
                     has_gun(state, player)
                 ))
    if "Train - Escape To Safety (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Train - Escape To Safety (Secret Agent)"):
        add_rule(world.multiworld.get_location("Train - Escape To Safety (Secret Agent)", player),
                 lambda state: (
                     state.has("Train Door Key (Depot Item)", player) and
                     has_gun(state, player)
                 ))
    if "Train - Key (Agent)" in world.location_name_to_id and is_valid_location(world, "Train - Key (Agent)"):
        add_rule(world.multiworld.get_location("Train - Key (Agent)", player),
                 lambda state: has_gun(state, player))
    if "Train - Key (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Train - Key (Double Agent)"):
        add_rule(world.multiworld.get_location("Train - Key (Double Agent)", player),
                 lambda state: has_gun(state, player))
    if "Train - Key (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Train - Key (Secret Agent)"):
        add_rule(world.multiworld.get_location("Train - Key (Secret Agent)", player),
                 lambda state: has_gun(state, player))
    if "Train - Locate Janus Secret Base" in world.location_name_to_id and is_valid_location(world, "Train - Locate Janus Secret Base"):
        add_rule(world.multiworld.get_location("Train - Locate Janus Secret Base", player),
                 lambda state: has_gun(state, player))
    if "Train - Locate Janus Secret Base (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Train - Locate Janus Secret Base (Double Agent)"):
        add_rule(world.multiworld.get_location("Train - Locate Janus Secret Base (Double Agent)", player),
                 lambda state: (
                     state.has("Train Door Key (Depot Item)", player) and
                     has_gun(state, player)
                 ))
    if "Train - Locate Janus Secret Base (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Train - Locate Janus Secret Base (Secret Agent)"):
        add_rule(world.multiworld.get_location("Train - Locate Janus Secret Base (Secret Agent)", player),
                 lambda state: (
                     state.has("Train Door Key (Depot Item)", player) and
                     has_gun(state, player)
                 ))
    if "Train - RC-P90" in world.location_name_to_id and is_valid_location(world, "Train - RC-P90"):
        add_rule(world.multiworld.get_location("Train - RC-P90", player),
                 lambda state: has_gun(state, player))
    if "Train - RC-P90 (Agent)" in world.location_name_to_id and is_valid_location(world, "Train - RC-P90 (Agent)"):
        add_rule(world.multiworld.get_location("Train - RC-P90 (Agent)", player),
                 lambda state: has_gun(state, player))
    if "Train - RC-P90 (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Train - RC-P90 (Double Agent)"):
        add_rule(world.multiworld.get_location("Train - RC-P90 (Double Agent)", player),
                 lambda state: has_gun(state, player))
    if "Train - RC-P90 (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Train - RC-P90 (Secret Agent)"):
        add_rule(world.multiworld.get_location("Train - RC-P90 (Secret Agent)", player),
                 lambda state: has_gun(state, player))
    if "Train - Rescue Natalya Again" in world.location_name_to_id and is_valid_location(world, "Train - Rescue Natalya Again"):
        add_rule(world.multiworld.get_location("Train - Rescue Natalya Again", player),
                 lambda state: has_gun(state, player))
    if "Train - Rescue Natalya Again (Agent)" in world.location_name_to_id and is_valid_location(world, "Train - Rescue Natalya Again (Agent)"):
        add_rule(world.multiworld.get_location("Train - Rescue Natalya Again (Agent)", player),
                 lambda state: (
                     state.has("Train Door Key (Depot Item)", player) and
                     has_gun(state, player)
                 ))
    if "Train - Rescue Natalya Again (Double Agent)" in world.location_name_to_id and is_valid_location(world, "Train - Rescue Natalya Again (Double Agent)"):
        add_rule(world.multiworld.get_location("Train - Rescue Natalya Again (Double Agent)", player),
                 lambda state: (
                     state.has("Train Door Key (Depot Item)", player) and
                     has_gun(state, player)
                 ))
    if "Train - Rescue Natalya Again (Secret Agent)" in world.location_name_to_id and is_valid_location(world, "Train - Rescue Natalya Again (Secret Agent)"):
        add_rule(world.multiworld.get_location("Train - Rescue Natalya Again (Secret Agent)", player),
                 lambda state: (
                     state.has("Train Door Key (Depot Item)", player) and
                     has_gun(state, player)
                 ))
    if "Train - Secret Agent (Clear)" in world.location_name_to_id and is_valid_location(world, "Train - Secret Agent (Clear)"):
        add_rule(world.multiworld.get_location("Train - Secret Agent (Clear)", player),
                 lambda state: (
                     state.has("Train Door Key (Depot Item)", player) and
                     has_gun(state, player)
                 ))

    for shared_name, per_difficulty_names in ITEM_SHARED_LOCATION_GROUPS.items():
        if shared_name not in world.location_name_to_id or not is_valid_location(world, shared_name):
            continue
        reachable_names = tuple(
            location_name
            for location_name in per_difficulty_names
            if location_name in world.location_name_to_id and is_valid_location(world, location_name)
        )
        if not reachable_names:
            continue
        add_rule(world.multiworld.get_location(shared_name, player),
                 lambda state, location_names=reachable_names: any(
                     state.can_reach_location(location_name, player)
                     for location_name in location_names
                 ))

    # Victory rules
    if world.options.mission_clear_mode.value == MISSION_CLEAR_MODE_PER_MAP:
        add_rule(world.multiworld.get_location("Stopped Goldeneye", player),
                 lambda state: has_gun(state, player))
    else:
        add_rule(world.multiworld.get_location("Stopped Goldeneye", player),
                 lambda state: (
                     has_gun(state, player) or  # Agent
                     has_gun(state, player) or  # Secret Agent
                     has_gun(state, player)  # Double Agent
                 ))

    world.multiworld.completion_condition[player] = lambda state: state.has("Victory", player)
